Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Flask-Cors==4.0.1
python-dotenv==1.0.1
Werkzeug==3.0.3

# STRIPE_SECRET_KEY / SENDGRID_API_KEY を設定して実送信・実決済を有効化する場合に必要。
# 未設定のままなら未インストールでも動く(billing/email_serviceが遅延importでフォールバックする)。
stripe==10.5.0
sendgrid==6.11.0

# テスト用
pytest==8.3.3
