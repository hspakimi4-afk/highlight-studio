# Highlight Studio — 製品化レビュー 引き継ぎメモ

最終更新: 2026-09-08(18回目の追加分 — 依頼者からの不具合報告2件
(累計アクセスカウンターが表示されない/書き出しが失敗する)をデバッグして
修正。詳細は末尾「35. 不具合修正: 累計アクセスカウンター表示 + 書き出し時の
ffmpeg同時実行エラー」を参照)

> **今回追加した内容の要約**: 依頼者から「カウンターが見つからない」
> 「無料で始めるの先のリンクがない」という2件の不具合報告があり調査・修正した。
> (1) `index.html`の累計アクセスカウンターは外部API(countapi.xyz)が
> サービス終了していたため常に非表示になっていた。外部依存をやめ、
> ブラウザ内(localStorage)完結のカウンターに変更し、あわせて依頼により
> ページ最上部にも同じカウンターを追加表示するようにした。
> (2) 「無料で始める」自体のリンクはコード上・実機検証とも問題なかったが、
> 依頼者からのスクリーンショットで実態が判明: 書き出しステップで
> 「ffmpeg.wasm can only run one command at a time」エラーにより書き出しが
> 失敗していた。`highlight-studio-03-app.html`のffmpeg呼び出しを共有キューで
> 直列化し、書き出し開始処理の二重起動防止ガードを追加して修正した。
> 依頼者による実機での再確認は依頼中。詳細は本ファイル末尾の
> 「35. 不具合修正: 累計アクセスカウンター表示 + 書き出し時のffmpeg同時実行
> エラー」を参照。
>
> **(過去の追加分)17回目**: 依頼者から「会員登録に関するものは全て削除。
> アップロード・編集・ダウンロードだけのサイトにし、広告視聴を都度挟んで
> 収益化する」という方針指示があり対応した。ログイン・新規登録・マイページ・
> 料金プランの4ページと、それを支えていたバックエンド一式(認証・決済)を
> 削除。アプリ本体(`03-app.html`)からもログイン必須処理・プラン制限を
> すべて撤去し、代わりに「書き出し開始時」「ダウンロード時」に広告視聴
> オーバーレイを挟む仕組みを実装した(広告ネットワーク未確定のため現状は
> カウントダウン付きのモック表示)。規約・プライバシーポリシーも会員登録
> 不要な運営形態に合わせて修正済み。詳細は本ファイル内
> 「34. 会員登録を全廃止 + 広告視聴ゲートによる収益化への転換」を参照。

## 1. これは何か

配信アーカイブから盛り上がり区間を自動検出し、縦動画・字幕付きクリップを
**ブラウザだけで**生成するサービス「Highlight Studio」の、静的HTMLサイト一式。
会員登録は不要で、「動画をアップロード → 編集(検出・字幕調整)→ ダウンロード」
だけで完結する。運営はバックエンドを持たず、広告視聴による収益化のみで成り立つ
構成(34章)。

現在のページ構成は7ページ(index / app本体 / terms / privacy / contact / about / 404)。
かつて存在した会員登録・ログイン・マイページ・料金プランの4ページと、それを
支えていたFlaskバックエンド一式は34章の対応で削除済み。

## 2. ページ構成(全7ファイル + アセット)

**注: 下記の表は本ファイル序盤(2回目のセッション)時点のものであり、現状と一致しない。
現在実在するファイルは `index.html`, `highlight-studio-03-app.html`,
`highlight-studio-07-terms.html`, `highlight-studio-08-privacy.html`,
`highlight-studio-10-contact.html`, `highlight-studio-11-about.html`, `404.html` の7ページ
(+ `robots.txt`/`sitemap.xml`/`i18n.js`/`i18n-common.js`/favicon類/`site.webmanifest`)のみ。
`02-auth`・`04-reset-password`・`05-mypage`・`06-pricing`・`09-tokushoho`・`api-client.js`・
`backend/`はいずれも34章までの対応で削除済み。最新の構成・変更点は34章を参照。**

| ファイル | 内容 | 状態 |
|---|---|---|
| `highlight-studio-01-landing.html` | ランディングページ | 既存 / フッター・OGP修正 |
| `highlight-studio-02-auth.html` | ログイン・新規登録 | 既存 / メール確認フロー追加 |
| `highlight-studio-03-app.html` | 本体(取込→検出→字幕→書き出し) | 既存 / a11y修正 |
| `highlight-studio-04-reset-password.html` | パスワード再設定 | 既存(変更なし) |
| `highlight-studio-05-mypage.html` | マイページ(プラン・請求・アカウント) | 既存 / プロフィール編集機能を追加 |
| `highlight-studio-06-pricing.html` | 料金プラン + FAQ | 既存 / フッター・OGP・FAQアンカー修正 |
| `highlight-studio-07-terms.html` | 利用規約 | 既存 / 特商法ページへのリンク追加 |
| `highlight-studio-08-privacy.html` | プライバシーポリシー | 既存 / フッター・OGP修正 |
| `highlight-studio-09-tokushoho.html` | **特定商取引法に基づく表記** | **新規** |
| `highlight-studio-10-contact.html` | **お問い合わせフォーム** | **新規** |
| `404.html` | 404エラーページ | **新規** |
| `robots.txt` / `sitemap.xml` | クローラ向け設定 | **新規** |
| `ogp.png` | SNSシェア用OGP画像(1200×630) | **新規(生成画像)** |
| `i18n.js` | 多言語(JA/EN)切り替え用の軽量i18nエンジン | **新規(2026-09-07)** |
| `i18n-common.js` | 全ページ共通の翻訳辞書(ナビ・フッター等) | **新規(2026-09-07)** |
| `favicon.ico` / `favicon-16x16.png` / `favicon-32x32.png` / `apple-touch-icon.png` / `icon-192.png` / `icon-512.png` | favicon一式(全サイズ) | **新規(2026-09-07)** |
| `site.webmanifest` | PWA向けマニフェスト | **新規(2026-09-07)** |

上記のうち `01`, `02`, `04`, `06`, `10`, `11`, `404.html` はJA/EN切り替えに対応済み(詳細は
「17. 多言語対応(JA/EN切り替え)着手」「23. 会社概要ページの多言語対応」を参照)。
`03`, `05`, `07`〜`09` は日本語のみ(07〜09は法律文書のため意図的に日本語のみ)。

## 3. 今回の対応内容

### 追加した機能
- **特定商取引法に基づく表記ページ**: 月額課金サービスとして法的に必須だが欠落していたため新規作成。販売事業者名・所在地などはプレースホルダーのため **公開前に実データへの差し替えが必須**(3-1参照)。
- **お問い合わせページ**: これまでmailtoリンクのみだった導線をフォーム化(バリデーション・送信完了画面つき、モック送信)。
- **メールアドレス確認フロー**: サインアップ後に「確認メールを送信しました」画面を挟むように変更。デモ用に「確認済みとして進む」ボタンで先に進める。
- **マイページのプロフィール編集**: 表示名・メールアドレス・パスワードの変更UIがなかったため追加(モーダル形式)。
- **SEO/シェア関連**: 主要ページに `og:image` / `og:url` / `canonical` を追加し、共通OGP画像(`ogp.png`)を生成。`robots.txt` / `sitemap.xml` を追加。
- **404ページ**を追加。

### 修正したバグ
- サインアップ時に入力したメールアドレスが `localStorage` に保存されず、マイページで常に「未設定」表示になっていた不具合を修正(`hs_userEmail` を正しく保存・参照するように変更)。
- お問い合わせページから料金プランFAQへのアンカーリンク `#faq` が実際には存在せず機能していなかった不具合を修正(`pricing.html` の該当セクションに `id="faq"` を付与)。
- `app.html` のハイライトボックス設定で、チェックボックスとテキスト入力が同一`<label>`内にあり、テキスト入力側にアクセシブルな名前が付与されていなかった問題を修正(`aria-label`追加、構造分離)。

### 整頓した点
- 全ページのフッターに「特定商取引法に基づく表記」「お問い合わせ」への導線を統一的に追加。
- 全10ページの内部リンク・アンカーIDを機械的に突き合わせ、リンク切れがないことを確認済み。
- 全ページのHTMLタグ対応・重複ID・インラインJS構文をスクリプトでチェック済み(異常なし)。

## 4. 未対応・引き継ぎ事項

### 4-1. 公開前に必須の対応
- `highlight-studio-09-tokushoho.html` 内の以下はプレースホルダー。実データに差し替えること:
  - 販売事業者名、運営統括責任者名、連絡先メールアドレス
  - `robots.txt` / `sitemap.xml` / OGP各タグの `https://highlight-studio.example.com` は仮ドメイン。本番ドメインに置換すること。

### 4-2. 実装が必要な箇所(現状すべてモック)
- 決済処理(現状はカード情報を入力するだけで実際の決済は行われない)
- メール送信基盤(確認メール・お問い合わせの自動返信・パスワード再設定メールなど、すべて「送信されません」という注記付きのモック)
- ユーザーデータの永続化(現状は各ブラウザの`localStorage`のみ。サーバー側DB・認証基盤が必要)
- 実際の動画解析・字幕生成パイプライン(`app.html`にffmpeg.wasm/whisper系の処理コードは組み込まれているが、本番運用時の負荷・対応フォーマット・エラーハンドリングは要検証)

### 4-3. 今回あえて手を付けなかった/判断が分かれる点
- 一部の細字テキスト(`text-faint` を `surface2` 背景上で使う箇所)がWCAG AAのコントラスト基準(4.5:1)をわずかに下回っている(実測 約4.02:1)。ブランドカラー全体に関わる変更のため、デザインレビューとセットでの対応を推奨し、今回は見送った。
- 多言語対応(英語版など)は未着手。

## 5. 今回の追加対応(アクセシビリティ)

- 4-3で保留されていた `text-faint` のコントラスト不足を修正。
  - `--text-faint` を `#707A8C` → `#7E88A0` に変更(全11ファイル、`:root` 定義箇所のみ)。
  - `surface2` (`#161A24`) 背景上での実測コントラスト比は 約4.02:1 → **約4.90:1** に改善し、WCAG AA(4.5:1)をクリア。
  - 変更は色相を保ったまま明度のみ調整しており、ブランドカラーの見た目への影響は最小限。
  - `text-faint` は他の背景(`surface`, `surface3` など、より明度差の大きい暗色背景)でも使用されているが、明るくする方向の変更なのでそれらのコントラストは悪化しない。

## 6. バックエンド実装ロードマップ(引き継ぎ先向けメモ)

現状すべてモック(4-2)の4項目について、実装に着手する際の推奨順序と方針:

1. **ユーザーデータ永続化・認証基盤**(最優先・他の3項目の土台になる)
   - `localStorage` 依存(`hs_userEmail` 等のキー)を洗い出し、サーバー側DB+セッション/JWT認証に置き換える。
   - `02-auth.html` のメール確認フロー(「確認済みとして進む」ボタン)は実メール送信基盤ができるまでの暫定UIなので、置き換え時に導線ごと見直すこと。

2. **決済処理**
   - `05-mypage.html` のプラン・請求UIと `06-pricing.html` のプラン定義が既にあるため、Stripe等の決済プロバイダのCheckout/Subscription APIと連携し、カード情報はプロバイダ側フォームに委譲(自前でカード番号を扱わない)のが安全。
   - 特商法ページ(09)の記載内容は決済確定前に法務観点で最終レビューが必要。

3. **メール送信基盤**
   - 確認メール・お問い合わせ自動返信・パスワード再設定メールの3系統。SendGrid/SESなど外部送信サービス経由を想定。
   - `10-contact.html` のモック送信・`02-auth.html` の確認メール文面は流用可能。

4. **動画解析・字幕生成パイプラインの本番検証**
   - `03-app.html` に組み込み済みのffmpeg.wasm/whisper系処理はブラウザ内実行のため、サーバー側で同等処理を行う場合は既存のFlask+ffmpeg/opencvバックエンドプロトタイプ(`backend/highlight_detector.py`, `exporter.py`, `app.py`)を土台に、対応フォーマット・処理時間・エラーハンドリングを本番負荷で検証する。
   - ブラウザ完結のまま公開する場合は、COOP/COEPヘッダーの本番サーバー設定が必須(4-2既出)。

### 6-1. バックエンド叩き台を追加(`highlight-studio-backend-skeleton.zip`)

上記ロードマップの1〜3(認証・データ永続化/決済/メール送信)について、Flaskベースの
叩き台を別リポジトリとして用意した。決済・メール送信の実処理は未実装(APIキー未設定時は
501を返すかログ出力のみのモック)だが、エンドポイント構成・DBモデル・フロント各ページとの
対応表は揃っている。詳細は同ZIP内の `README.md` を参照。

- フロントとは別デプロイ(別オリジン)を想定し、CORSで接続する構成。
- 特商法ページの事業者情報は `.env` の `BUSINESS_NAME` 等に環境変数化してあるので、
  実データが揃ったら `.env` と `09-tokushoho.html` の両方に反映すること(現状09は
  静的HTMLなので自動連携はしない)。
- 動画解析パイプライン(ロードマップ4)は今回のスケルトンには含めていない。既存の
  Flask+ffmpeg/opencvプロトタイプ側の資産として別管理のまま。

### 6-1-1. Stripe/SendGridの実装・pytestスイートを追加

`billing.py`/`email_service.py` はコメントの実装例ではなく、実際に動く実装(Checkout
Session作成・Webhook処理・Stripeサブスク状態に応じたプラン更新、SendGrid実送信)に差し替えた。
どちらもキー未設定・パッケージ未インストール時は既存のモック/501フォールバックに落ちるので、
このまま渡しても壊れない。`tests/`にauth・contact・billingのpytestスイートも追加。

**重要**: このサンドボックス環境は外部ネットワークアクセスができず、`flask-sqlalchemy` 等
依存パッケージのインストールも `stripe`/`sendgrid` パッケージの動作確認もできていない。
全ファイルの構文チェック(`python -m py_compile`)は通しているが、`pip install -r
requirements.txt && pytest` を一度手元で実行して結果を確認してから使うこと。

### 6-2. フロント↔バックエンドの接続を実装(`api-client.js`)

`highlight-studio-productized.zip` に `api-client.js` を追加し、以下4ページの該当箇所を
バックエンドAPIと接続できるように書き換えた。**デフォルトでは何もしないフィーチャーフラグ方式**
になっており、`window.HS_API_BASE` を設定しない限り、これまで通り `localStorage` だけで
動くデモ動作のまま(挙動は変えていない)。

| ページ | 接続した処理 |
|---|---|
| `02-auth.html` | ログイン(`/api/auth/login`)、新規登録(`/api/auth/signup`) |
| `04-reset-password.html` | パスワード再設定リクエスト・実行(`/api/auth/request-password-reset`, `/api/auth/reset-password`。URLの`?token=`を利用) |
| `05-mypage.html` | プロフィール編集の表示名・パスワード変更(`PATCH /api/auth/me`。メールアドレス変更は6-1同様まだ未対応) |
| `10-contact.html` | お問い合わせフォーム送信(`POST /api/contact`) |

**有効化する場合**は、各ページの `<script src="./api-client.js"></script>` より前で
`window.HS_API_BASE = 'https://api.example.com';` のように設定するだけでよい。
CORSはバックエンド側 `.env` の `ALLOWED_ORIGINS` にフロントのオリジンを追加すること。

各ファイルの追加・変更部分は `node --check` でインラインJSの構文確認済み(全4ファイルOK)。
ただし**実際にバックエンドを起動しての結合テストは今回の環境では未実施**(このサンドボックスは
外部ネットワーク・ローカルサーバー起動を伴う検証ができないため)。接続を有効化する際は、
最低限ログイン→マイページ遷移、新規登録→メール確認(デモ継続ボタン)、お問い合わせ送信の
3導線を手動で一通り確認すること。

## 7. 動作確認方法

このZIPを展開し、`highlight-studio-01-landing.html` をブラウザで直接開けば一通り遷移できる(サーバー不要、すべて相対パス・ローカルストレージで完結)。

- 新規登録 → メール確認画面 → 「確認済みとして進む」 → アプリ本体
- マイページ → 「プロフィールを編集」で表示名・メール変更を確認可能
- 各ページ最下部フッターから 特商法/お問い合わせ ページへ遷移可能

## 8. デバッグ用管理者アカウントを追加(2026-09-07)

動作確認・デバッグを無制限・無料で行えるアカウントが欲しい、という要望に対応。
フロントのデモ動作(localStorageのみ)と、バックエンド叩き台の両方に追加した。

### 8-1. フロント(`highlight-studio-productized.zip`)

- `highlight-studio-02-auth.html` のログインフォームに、ハードコードされた管理者用
  メールアドレス・パスワード(`ADMIN_CREDENTIALS` 変数、現状 `admin@highlight-studio.local` /
  `debug-admin-2026`)を追加。これでログインすると、バックエンド接続(`window.HS_API_BASE`)の
  有無に関わらずAPIを呼ばずに `localStorage` へ直接「管理者プラン」状態を書き込んでアプリ本体へ
  遷移する(`hs_plan='admin'`, `hs_isAdmin='1'`)。
- `highlight-studio-03-app.html`: `PLAN_LIMITS` に `admin: Infinity` を追加。プロプラン限定
  機能(高精度音声認識モデル)のロック判定を `admin` にも解放。無料プランのロック・透かし処理は
  元々 `currentPlan === 'free'` 判定のブロック内にまとまっていたため、`admin` は自動的に対象外
  になる(コード変更不要)。
- `highlight-studio-05-mypage.html`: プラン表示の `PLANS` オブジェクトに
  `admin: { name: '管理者(デバッグ用)', price: '無料・無制限', ... }` を追加。管理者プランの
  場合はプラン変更・お支払い方法管理のボタンを非表示にし、課金導線に迷い込まないようにした。
- ログアウト・アカウント削除の各処理に `localStorage.removeItem('hs_isAdmin')` を追加。
- 影響範囲は上記3ファイルのみ。既存の一般ユーザー導線(無料/スタンダード/プロ)の挙動は変更していない。

### 8-2. バックエンド(`highlight-studio-backend-skeleton.zip`)

- `app/models.py` の `User` に `is_admin`(Boolean, default False)を追加。
  `effective_plan` プロパティで「`is_admin` なら常に `"admin"`、それ以外は `plan` カラムの値」
  を返すようにし、`to_public_dict()` の `plan` はこの `effective_plan` を返す
  (課金プラン自体を表す `plan` カラムはStripe連携用にそのまま残してあるので、
  管理者フラグを外せば元の課金プランへそのまま戻る)。
- `app/config.py` / `.env.example` に `ADMIN_EMAIL` / `ADMIN_PASSWORD` を追加。
- `app/__init__.py` に `_ensure_admin_account()` を追加し、起動時(`create_app()` 内、
  `db.create_all()` の直後)に上記環境変数が両方設定されていれば該当ユーザーを
  `is_admin=True` で自動作成(既存なら更新)する。未設定時は何もしない(本番のデフォルト)。
- 決済(`billing.py`)・メール送信のロジックは変更していない。管理者ユーザーもStripe上は
  通常ユーザーと同様に扱われる想定(`plan`カラムへの操作はそのまま)。

### 8-3. 引き継ぎ・公開前の注意

- フロントのハードコードされた管理者アカウント(`highlight-studio-02-auth.html` 内
  `ADMIN_CREDENTIALS`)は、そのまま公開するとログインフォームから誰でも無制限プランに
  なれてしまう。**公開前に必ず値を変更するか、このブロック自体を削除すること。**
- バックエンドの `ADMIN_EMAIL` / `ADMIN_PASSWORD` も同様に、デバッグが終わったら
  `.env` から空にするか、作成された管理者ユーザーをDBから削除すること。
- フロントの管理者ログインとバックエンドの管理者アカウントは、現状連動していない
  (フロントは`localStorage`への直接書き込みのみでAPIを呼ばないため)。将来的に
  フロント↔バックエンド接続(`api-client.js`)を有効化する場合は、フロントの
  ハードコード判定を外し、バックエンドの管理者アカウントでログインする形に統一する
  ことを推奨する。
- 本対応のみで完結しており、既存の未対応事項(4章)・ロードマップ(6章)の内容に変更はない。

## 9. 字幕スタイル・レイアウトの作り込み(2026-09-07)

4-3・6章で「今後の課題」として残っていた字幕・レイアウトの作り込みについて、
`highlight-studio-03-app.html`(STEP3: 字幕確認・修正画面)に以下を追加した。

### 9-1. 追加した機能

- **字幕の文字サイズ選択**: 「小さめ / 標準 / 大きめ」の3段階セグメントを追加
  (`capSizeSegmented`)。倍率は `CAPTION_SIZE_MULT = { small:0.8, standard:1, large:1.25 }` で、
  スタイル1・スタイル2それぞれの基準フォントサイズに掛け合わせる(`getCaptionStyle()`)。
- **字幕の背景帯(半透明ボックス)**: チェックボックス(`capBgBoxCheck`)をONにすると、
  drawtextに `box=1:boxcolor=black@0.45:boxborderw=...` を追加し、明るい映像の上でも
  字幕が読みやすくなるようにした。OFF時は従来通り縁取りのみ。
- **長い字幕の自動折り返し**: `wrapCaptionText()` を追加。動画の横幅(選択した画質プリセットの
  `preset.scale` から取得)とフォントサイズから1行あたりの目安文字数を計算し、
  最大3行までに自動で折り返す(全角文字の幅をfontsizeとほぼ同じとみなす簡易計算)。
  3行に収まらない場合は末尾を省略記号「…」で切り詰める。ffmpegのdrawtextは `text_h`(`th`)が
  複数行時も全体の高さを表すため、既存の `y='h-th-50'` 等の位置指定はそのまま複数行でも
  正しく機能する(変更不要だった)。
- ミニプレビュー(STEP3下部の縦長プレビュー)にも文字サイズ・背景帯の見た目を反映するCSSクラス
  (`.size-small` / `.size-large` / `.bgbox`)を追加し、選択内容が視覚的にわかるようにした。

### 9-2. 対象範囲・影響

- 変更は `highlight-studio-03-app.html` のみ(CSS・HTML・インラインJS)。他ページへの影響なし。
- 既存の字幕スタイル(顔出しカメラ型/イラスト+テロップ型)の判定ロジック・色分けはそのまま。
  文字サイズ・背景帯は両スタイル共通の追加オプションとして乗せてある。
- スタイル2の画面上部固定ハイライトボックス(黄色い帯)は今回の対象外(変更していない)。
- 構文チェック(`node --check` でインラインJS抽出分)は通過済み。実際のffmpeg.wasm実行結果
  (複数行drawtextの見た目、折り返し文字数の妥当性)はこのサンドボックス環境では確認できて
  いないため、ブラウザで一度長めの字幕を使って実際の見え方を確認することを推奨する。

### 9-3. 今後さらに作り込むなら

- 折り返しの文字数計算は簡易的な固定比率(fontsizeとほぼ同じ幅)のため、英数字が多い字幕では
  やや余裕を持ちすぎる/日本語が多いと詰まりすぎる、といった誤差が出うる。精度を上げるなら
  Canvas の `measureText()` 等で実際のフォント幅を測ってから折り返し位置を決める方式に
  置き換えるとよい。
- 字幕の文字色・縁取り色をスタイルごとの固定値からユーザー選択式にする、字幕の表示位置
  (上/中央/下)を選べるようにする、といった拡張も考えられる → **今回(10章)で対応した**。

## 10. 字幕の表示位置・文字色の選択式化(2026-09-07)

9章で「今後の課題」として残っていた、字幕の表示位置と文字色の選択式化に対応した。
対象は `highlight-studio-03-app.html`(STEP3: 字幕確認・修正画面)のみ。

### 10-1. 追加した機能

- **字幕の表示位置**: 「上 / 中央 / 下(既定)」の3段階セグメント(`capPosSegmented`)を追加。
  - 「下(既定)」を選んだ場合は、従来通りレイアウトスタイルごとの既定y座標
    (顔出しカメラ型: `h-th-50`、イラスト+テロップ型: `h-th-160`)をそのまま使う。
  - 「中央」は `y=(h-th)/2`、「上」は `y=110`(画面上端から一定オフセット)に固定。
  - `CAPTION_POSITION_Y` というマップに逃がしてあるだけなので、オフセット値の微調整は
    このマップの値を書き換えるだけでよい。
  - イラスト+テロップ型でハイライトボックス(画面上部固定・黄色い帯)を表示している場合、
    「上」を選ぶと字幕と重なることがある。これは意図的にそのままにしてあり、
    field-note(画面上の説明文)で注意書きのみ行っている(自動回避ロジックは未実装)。
- **字幕の文字色**: 「既定 / 白 / 黄色 / ピンク / 水色 / 黄緑」の6択セグメント
  (`capColorSegmented`)を追加。各ボタンに色見本の丸(`.dot`)を表示。
  - 「既定」を選んだ場合は、従来通りレイアウトスタイルごとの既定色
    (顔出しカメラ型: 白、イラスト+テロップ型: ピンク)を使う。
  - 色名はffmpeg drawtextがそのまま解釈できる標準色名(`white`/`yellow`/`hotpink`/`cyan`/
    `lime`)を採用しているため、`fontcolor=` に直接渡すだけで動作する(色コード変換は不要)。
- ミニプレビュー(STEP3下部の縦長プレビュー)にも反映: 位置は`.pos-top`/`.pos-center`
  クラスで文字の縦位置を、文字色は該当色を`style.color`に直接セットして表示する
  (どちらも「イラスト+テロップ型」のプレビュー、つまりハイライトボックスの見た目には
  影響させないよう `:not(.style2)` 等で除外している)。

### 10-2. 実装方針・互換性について

- `getCaptionStyle()` の返り値に `y`(位置)と `fontcolor`(色)の上書きロジックを追加した
  だけで、既存の文字サイズ倍率・折り返し・背景帯のロジックには一切手を入れていない。
- **後方互換**: 表示位置は「下(既定)」、文字色は「既定」がそれぞれ初期選択されており、
  この2つが選ばれている限り出力結果は前回(9章)の版と完全に同一(y座標も色も同じ値になる)。
  何もしなければ挙動は変わらない。
- 対象は `highlight-studio-03-app.html` のみ。他ページへの影響なし。

### 10-3. 動作確認状況

- インラインJSの構文チェック(`node --check`)は通過済み。
- 実際のffmpeg.wasm実行結果(選んだ位置・色が焼き込み後の縦動画に正しく反映されるか)は
  このサンドボックス環境では確認できていないため、ブラウザで一度、位置・色を変えて
  実際に書き出し、見た目を確認することを推奨する。

### 10-4. 今後さらに作り込むなら

- 「上」の固定オフセット(`y=110`)は顔出しカメラ型・イラスト+テロップ型で共通の値にして
  あるため、イラスト+テロップ型でハイライトボックスと重なる場合の自動回避(ハイライト
  ボックス表示中は「上」を選べなくする、またはさらに下にオフセットする等)は未実装。
- 文字色は現状プリセット6色のみ。カラーピッカーによる自由指定にする拡張も考えられる。
- 縁取り色(`bordercolor`)は今回の対象外(スタイルごとの固定値のまま)。

## 11. 字幕の折り返し精度向上(2026-09-07)

10-4で「今後さらに作り込むなら」として挙げていた、折り返し文字数計算の精度向上に対応した。
対象は `highlight-studio-03-app.html` の `wrapCaptionText()` および周辺の追加関数のみ。

### 11-1. 変更内容

- 従来は「1文字あたりfontsizeとほぼ同じ幅」という固定比率で1行あたりの文字数を計算していた
  (英数字が多いと余裕を持ちすぎる/日本語が多いと詰まりすぎるという誤差があった)。
- 今回、実際に焼き込みに使うフォント(`NotoSansJP-Regular.ttf`)を`FontFace` APIでブラウザに
  読み込み、Canvasの`measureText()`で1文字ずつ実測した幅をもとに、貪欲法(greedy)で
  1行の幅(`videoWidthPx - 80`相当)に収まるだけ文字を詰めていく方式に置き換えた
  (`buildCharMeasurer()` → `wrapCaptionText()`)。
  - 全角・半角が混ざった字幕(例:「OMG this is insane 神展開すぎる件」)でも、
    文字ごとの実際の幅で折り返し位置が決まるため、行の見た目の詰まり具合が改善される。
  - 3行に収まらない場合の省略記号「…」も、末尾の実測幅を見ながら必要な分だけ文字を削って
    付与するようにした(以前は文字数ベースの単純な切り詰めだった)。
- **フォールバック**: `FontFace`未対応のブラウザ、またはフォント読み込みに失敗した場合は、
  従来に近い概算幅(全角相当=fontsizeとほぼ同じ幅、半角英数記号=その約55%)にそのまま
  フォールバックする。読み込み失敗時は動作ログに理由を出力するのみで、処理自体は止めない。
- フォント読み込みは`runBurnAll()`内で、ffmpeg用にフォントファイルを取得した直後
  (`ensureCaptionFontLoaded()`)に1回だけ行い、以降のクリップ・字幕行の折り返し計算で
  使い回す。

## 12. 字幕折り返しの英単語単位化(2026-09-07)

11-4で「今後さらに作り込むなら」として挙げていた、英単語が途中で改行されてしまう問題に
対応した。対象は `highlight-studio-03-app.html` の折り返しロジック(`wrapCaptionText()`
まわり)のみ。

### 12-1. 変更内容

- 文字を1つずつ詰めていく方式(11章)から、まずテキストを「トークン」に分割してから
  詰めていく方式に変更した(`tokenizeForWrap()`)。
  - 半角英数字(アポストロフィ・ハイフン含む)の連続は1つの単語として1トークンにまとめる
    (例:「insane」は分割せず1トークンとして扱う)。
  - 空白はまとめて1トークン。日本語などそれ以外の文字は、従来通り1文字ずつ1トークン。
- 行に詰め込む際(`wrapTokensToLinesUnbounded()`)、英単語トークンは基本的に1行の幅に
  収まる限りそのまま乗せ、収まらない場合はトークンごと次の行に送る(単語の途中では
  改行しない)。行末の空白は捨てて改行するため、行頭に余計な空白が入ることもない。
  - 例外: 1つの単語だけで1行分の幅を超えてしまう場合(非常に長い英単語や、画面幅が
    極端に狭い場合)のみ、その単語だけは従来通り文字単位で強制的に分割する
    (無限ループや画面はみ出しを避けるための保険)。
- 3行に収まらない場合の省略記号処理は、単純に「4行目以降に相当する部分」を元のテキストの
  該当箇所からそのまま切り出し、実測幅で末尾を削って「…」を付ける方式に変更した。
  - この省略記号での切り詰めのみ、見た目(画面内に収める)を優先しているため、
    英単語の途中で終わることがある(あくまで3行に収まりきらない場合の例外処理)。

### 12-2. 対象範囲・互換性

- 変更は `highlight-studio-03-app.html` のみ(CSS・HTMLの変更なし、インラインJSのみ)。
  他ページへの影響なし。
- `wrapCaptionText(text, fontsize, videoWidthPx)` という関数のシグネチャ(呼び出し側の
  インターフェース)は変更していないため、呼び出し箇所(`runBurnAll()`内)の修正は不要。
- 文字サイズ・表示位置・文字色・背景帯・実フォント幅の測定ロジック(`buildCharMeasurer()`、
  `ensureCaptionFontLoaded()`)自体は変更していない。11章の内容の上に、詰め込みの単位を
  「文字」から「トークン(単語/空白/1文字)」に変えただけ。
- 日本語のみの字幕(英単語を含まない)の折り返し結果は、11章の版と実質的に変わらない
  (日本語は従来通り1文字単位でトークン化されるため)。英数字を含む字幕でのみ、
  単語の途中で改行されなくなるという違いが出る。

### 12-3. 動作確認状況

- ロジック自体は、実測幅を固定値に差し替えたモックでNode.js上で動作確認済み
  (英日混在文で単語が割れないこと、非常に長い1単語のみ強制分割されること、
  日本語のみの長文で従来通り3行+省略記号になること、3行に収まらない英日混在文で
  想定通り末尾が切り詰められることの4パターンを確認)。
- インラインJSの構文チェック(`node --check`)は通過済み。IDの重複なし。
- **実際のブラウザでの動作(FontFace読み込み・measureTextの実測値・ffmpeg.wasmでの
  焼き込み結果)は、このサンドボックス環境では確認できていない**。11章と同様、
  公開前に英数字を含む長めの字幕で一度実際に書き出し、見た目を確認することを推奨する。

### 12-4. 今後さらに作り込むなら

- 現状、単語の区切りは半角英数字の連続のみを対象にしている。将来的にハイフン区切りの
  複合語(例:「well-known」)を単語の途中(ハイフンの位置)で改行してよいことにする、
  といった細かい調整は今回の対象外。
- 3行に収まらない場合の省略記号処理(12-1末尾)は、あえて単語区切りを気にせず
  実測幅だけで切り詰める仕様にしてある。この挙動を変え、省略記号側でも単語の途中で
  切らないようにしたい場合は、`remainderText`を切り詰める際にもトークン境界を
  意識したロジックに置き換える必要がある。

## 13. 字幕の縁取り色を選択式化(2026-09-07)

10-4で「今後の対象外」としていた縁取り色(`bordercolor`)について、文字色(10章)と
同様に選択式にした。対象は `highlight-studio-03-app.html` のみ。

### 13-1. 追加した機能

- **字幕の縁取り色**: 「既定(黒) / 黒 / 白 / 濃紺」の4択セグメント
  (`capBorderColorSegmented`)を、文字色セグメントのすぐ下に追加。各ボタンに色見本の丸
  (`.dot`)を表示。
  - 「既定(黒)」を選んだ場合は、従来通りスタイル共通の黒(`bordercolor:'black'`)を使う
    (顔出しカメラ型・イラスト+テロップ型とも、これまで縁取り色は常に黒だった)。
  - 色名はffmpeg drawtextがそのまま解釈できる標準色名(`black`/`white`/`navy`)を採用して
    いるため、`bordercolor=` に直接渡すだけで動作する(文字色と同じ実装方針)。
  - field-noteに「文字色を明るくしたら縁取りは黒のまま、文字色を暗めにするなら縁取りを
    白に」という簡単な目安を記載した。
- ミニプレビューにも反映: `style !== 'style2'` の場合のみ、選んだ縁取り色を
  `-webkit-text-stroke` のインライン指定として反映する(イラスト+テロップ型の
  プレビュー、つまりハイライトボックスの見た目には影響させない。文字色と同じ方針)。

### 13-2. 実装方針・互換性について

- `getCaptionStyle()` の返り値に `bordercolor` の上書きロジックを追加しただけで、
  文字色・サイズ・位置・折り返しなど既存のロジック(9〜12章)には一切手を入れていない。
- **後方互換**: 「既定(黒)」が初期選択されており、これが選ばれている限り出力結果は
  前回(12章)の版と完全に同一(縁取り色は常に黒のまま)。何もしなければ挙動は変わらない。
- 対象は `highlight-studio-03-app.html` のみ。他ページへの影響なし。

### 13-3. 動作確認状況

- インラインJSの構文チェック(`node --check`)は通過済み。IDの重複なし。
- 実際のffmpeg.wasm実行結果(選んだ縁取り色が焼き込み後の縦動画に正しく反映されるか)は
  このサンドボックス環境では確認できていないため、ブラウザで一度、縁取り色を変えて
  実際に書き出し、見た目を確認することを推奨する。

### 13-4. 今後さらに作り込むなら

- 現状プリセット3色(黒・白・濃紺)のみ。文字色と同様、カラーピッカーによる自由指定に
  する拡張も考えられる。
- 文字色と縁取り色の組み合わせによっては視認性が下がる場合がある(例:白文字+白縁取り)
  が、現状は組み合わせのバリデーション(警告表示など)は行っていない。

### 11-2. 対象範囲・互換性

- 変更は `highlight-studio-03-app.html` のみ(CSS・HTMLの変更なし、インラインJSのみ)。
  他ページへの影響なし。
- 文字サイズ・表示位置・文字色・背景帯など、既存の字幕まわりのオプション(9章・10章)には
  一切手を入れていない。`wrapCaptionText(text, fontsize, videoWidthPx)`という関数の
  シグネチャ(呼び出し側のインターフェース)も変更していないため、呼び出し箇所の修正は不要。
- 出力される折り返し結果(何文字目で改行されるか)は、フォント読み込みが成功する環境では
  以前と変わる(=精度が上がる分、見た目が変わる)。フォールバック時の概算式は以前とほぼ
  同じ考え方だが、全角/半角判定を追加した分だけ以前より若干精度が上がっている。

### 11-3. 動作確認状況

- ワードラップのロジック自体は、実測幅を固定値に差し替えたモックでNode.js上で
  動作確認済み(短文/長文/全角半角混在/同一文字大量繰り返しの4パターンで、行の折り返し・
  省略記号付与が期待通りになることを確認)。
- インラインJSの構文チェック(`node --check`)は通過済み。IDの重複なし。
- **実際のブラウザでの`FontFace`読み込み・`measureText()`の実測値、およびffmpeg.wasmでの
  焼き込み結果(実際の見た目)は、このサンドボックス環境では確認できていない**。公開前に、
  日本語・英数字混在の長めの字幕を含む動画で一度実際に書き出し、行の折り返し位置・
  省略記号の見え方を確認することを強く推奨する。

### 11-4. 今後さらに作り込むなら

- 現状は「1文字ずつ幅を足していく」貪欲法のみで、単語(特に英単語)の途中で改行される
  ことがある。英語字幕を重視するなら、スペース単位で区切ってから詰める方式(単語の
  途中では折り返さない)に拡張するとよい。
- フォント読み込みはクリップ・ページ全体で1回のみ(`_hsCaptionFontReady`でキャッシュ)。
  将来的にフォントファイル自体を差し替え可能にする場合は、このキャッシュのリセットが
  必要になる点に注意。

## 14. 字幕の文字色・縁取り色のカラーピッカー対応(2026-09-07)

13-4・10-4で「今後さらに作り込むなら」として挙げていた、文字色・縁取り色をプリセットだけで
なく自由指定にする拡張に対応した。対象は `highlight-studio-03-app.html` のみ。

### 14-1. 追加した機能

- 文字色セグメント(`capColorSegmented`)・縁取り色セグメント(`capBorderColorSegmented`)の
  それぞれ末尾に「カスタム」ボタンを追加。選択すると、その場に `<input type="color">` の
  行が現れ、任意の色をカラーピッカーで指定できる。
  - 文字色の初期値は白(`#ffffff`)、縁取り色の初期値は黒(`#000000`)。
  - 色を選ぶたびにミニプレビュー(STEP3下部)へ即時反映される(`input`イベント)。
  - 「カスタム」ボタンの色見本(`.dot`)は、選択前は虹色のグラデーションアイコンで
    「自由に選べる」ことを示し、実際に色を選ぶとその色に切り替わる。
- 色の受け渡しは、カラーピッカーが返す`#RRGGBB`形式の文字列をそのまま使っている。
  ffmpeg drawtextの`fontcolor`/`bordercolor`は`#RRGGBB`形式をそのまま解釈できるため、
  プリセット(色名文字列)とカスタム(`#RRGGBB`)が混在していても変換処理は不要
  (`resolveCustomColor()`という共通関数に集約した)。

### 14-2. 実装方針・互換性について

- `getCaptionStyle()` 内の色解決ロジックを`resolveCustomColor()`という関数に共通化し、
  文字色・縁取り色どちらも「プリセット選択時はプリセット値、カスタム選択時はカラーピッカーの
  値」を返すようにしただけで、既存のプリセット(白/黄色/ピンク/水色/黄緑、黒/白/濃紺)や
  「既定」の挙動・文字サイズ・位置・折り返し・背景帯のロジックには一切手を入れていない。
- **後方互換**: 「既定」が初期選択されている点は変わらないため、「カスタム」を選ばない限り
  出力結果は前回(13章)の版と完全に同一。何もしなければ挙動は変わらない。
- 対象は `highlight-studio-03-app.html` のみ。他ページへの影響なし。CSS・HTML・インラインJSの
  変更のみ。

### 14-3. 動作確認状況

- インラインJSの構文チェック(`node --check`)は通過済み。IDの重複なし(新規追加した
  `capColorCustomRow`/`capColorCustomPicker`/`capColorCustomDot`/`capBorderColorCustomRow`/
  `capBorderColorCustomPicker`/`capBorderColorCustomDot`を含め、全72個のidが重複なしであることを
  確認)。
- 実際のffmpeg.wasm実行結果(カラーピッカーで選んだ色が焼き込み後の縦動画に正しく反映されるか、
  `#RRGGBB`形式がffmpeg側で問題なく解釈されるか)は、このサンドボックス環境では確認できて
  いないため、ブラウザで一度、カスタム色を選んで実際に書き出し、見た目を確認することを
  推奨する。

### 14-4. 今後さらに作り込むなら

- 文字色と縁取り色をどちらもカスタムにした場合の組み合わせバリデーション(13-4で既出の
  「白文字+白縁取り」のような視認性低下の警告表示など)は未実装のまま。
- カラーピッカーの初期値(文字色=白、縁取り色=黒)は固定値。レイアウトスタイル
  (顔出しカメラ型/イラスト+テロップ型)の既定色に連動させる(例:「既定」から「カスタム」に
  切り替えた瞬間、直前の既定色をピッカーの初期値にする)といった細かい作り込みも考えられるが、
  今回の対象外とした。

## 15. 字幕「上」位置とハイライトボックスの重なり自動回避(2026-09-07)

10-4で「未実装」として残っていた、「イラスト+テロップ型」でハイライトボックスを表示中に
字幕位置を「上」にすると重なってしまう問題に対応した。対象は `highlight-studio-03-app.html`
の `getCaptionStyle()` と、ハイライトボックスの drawtext 生成部分(`runBurnAll()`内)のみ。

### 15-1. 変更内容

- ハイライトボックスの位置・サイズ(`fontsize:30, y:50, boxborderw:14`)を
  `HIGHLIGHT_BOX`という定数にまとめ、ハイライトボックスのdrawtext生成側もこの定数を
  参照するように変更(値そのものは従来と同じなので見た目は変わらない)。
- ハイライトボックスは常に1行(改行なし)でテキスト量に関わらず高さが一定であることを
  利用し、ボックス下端のy座標を`HIGHLIGHT_BOX_BOTTOM_Y`として計算式で求めた
  (`y + fontsize×1.2(行の高さ概算) + boxborderw`)。
- 字幕位置が「上」、かつ「イラスト+テロップ型」でハイライトボックスのチェックがON、
  かつハイライトボックスのテキスト欄に入力がある(=実際にボックスが焼き込まれる)場合のみ、
  字幕のy座標を`HIGHLIGHT_BOX_BOTTOM_Y + 30px`(余白)まで自動的に下げるようにした
  (`isHighlightBoxActive()`で判定)。
  - それ以外の組み合わせ(顔出しカメラ型/ハイライトボックス非表示/テキスト未入力など)では
    従来通り`y=110`のまま変更なし。
- 表示位置フィールドの注記文を「重なることがあります」→「自動でよける位置に焼き込まれます」
  に更新した。

### 15-2. 対象範囲・互換性

- 変更は `highlight-studio-03-app.html` のみ(HTMLは注記文の1行のみ変更、他はインラインJS)。
- ハイライトボックス自体の見た目(位置・フォントサイズ・枠の余白)は変更していない
  (定数化しただけで値は同じ)。
- 「上」以外の位置(中央・下)、および顔出しカメラ型の字幕位置ロジックには一切影響しない。
- STEP3のミニプレビュー(`capPreviewMiniBox`)は、元々イラスト+テロップ型選択時は
  ハイライトボックスの見た目のみを表示する簡易表示になっており、字幕の表示位置(上/中央/下)
  を反映していない仕様だったため、今回もプレビュー側の見た目は変えていない
  (実際の焼き込み結果にのみ反映される)。

### 15-3. 動作確認状況

- インラインJSの構文チェック(`node --check`)通過、IDの重複なし(既存72個から変化なし)。
- 計算式の検算: `y:50, fontsize:30, boxborderw:14` の場合、ボックス下端は約100px、
  自動回避後の字幕yは130pxとなり、十分な余白でボックスの下に収まることを確認。
- 実際のffmpeg.wasm書き出し結果(縦動画上で本当に重ならず見やすい位置になっているか)は
  このサンドボックス環境では確認できていないため、ブラウザで一度、イラスト+テロップ型+
  ハイライトボックスON+字幕位置「上」の組み合わせで書き出し、見た目を確認することを推奨する。

### 15-4. 今後さらに作り込むなら

- 現状、回避後のy座標(130px)は固定値。字幕サイズ(小さめ/標準/大きめ)によって字幕自体の
  高さも変わるため、大きめサイズ選択時に画面上部で窮屈にならないか等は未検証。
- ハイライトボックスのテキストが極端に長く、画面幅を超えて自動的に縮小・折り返しされるような
  仕様に将来変更する場合は、`HIGHLIGHT_BOX_BOTTOM_Y`の計算式(1行固定を前提にしている)も
  合わせて見直す必要がある。

## 16. 文字色・縁取り色の組み合わせ警告表示(2026-09-07)

14-4で「未実装」として残っていた、文字色と縁取り色の組み合わせによっては視認性が下がる
(例:白文字+白縁取り)ケースの警告表示に対応した。対象は `highlight-studio-03-app.html` のみ。

### 16-1. 追加した機能

- 字幕の縁取り色フィールドの下に、警告文用の注記(`#capColorContrastWarning`)を追加。
  文字色・縁取り色の実際の組み合わせが「近い色」と判定された場合のみ表示される
  (`.field-note`よりやや目立つアクセントカラーで表示。ブロッキングはせず、あくまで注意喚起)。
- 判定は、文字色・縁取り色それぞれの実際に使われる色(プリセット/カスタム/「既定」の
  フォールバック色すべて含む)をRGBに変換し、単純なユークリッド距離(0〜約441.7)で
  近さを測る簡易的な方式(`colorDistance()`)。距離が90未満の場合に警告を表示する
  (`isLowContrastCaptionColor()` / `LOW_CONTRAST_DISTANCE_THRESHOLD`)。
  - 厳密なコントラスト比(WCAG等)の計算ではなく、あくまで「同じ色・非常に近い色」を
    検出するための簡易判定である点に注意。
  - 検証: 白×白/黒×黒/ほぼ白同士(#eeeeee×#ffffff)は警告、既定の白×黒・ピンク×黒・
    黄色×黒・水色×白・黄緑×濃紺などは警告なし、という結果になることをNode上で確認済み
    (本HANDOFF作成時点の検算)。
- 文字色・縁取り色のセグメント選択、カラーピッカーでの色変更、レイアウトスタイル
  (顔出しカメラ型/イラスト+テロップ型)の切り替えのいずれでも、既存の
  `updateHighlightBoxVisibility()`内で判定し直され、警告表示がリアルタイムに更新される。

### 16-2. 実装方針・リファクタリングについて

- 文字色・縁取り色の「実際に使われる色」を解決するロジック(プリセット/カスタム/既定の
  フォールバック)が、これまで`getCaptionStyle()`内にしかなかったため、警告判定でも
  同じロジックが必要になった。重複を避けるため`resolveEffectiveCaptionColors(styleVal)`
  という共通関数に切り出し、`getCaptionStyle()`・警告判定の両方から呼び出す形にリファクタリング
  した。**この関数が返す値(fontcolor/bordercolor)自体は、リファクタリング前の
  `getCaptionStyle()`が計算していた値と完全に同一であり、焼き込み結果への影響はない。**
- 上記のリファクタリングに伴い、`CAPTION_STYLES`定数や`getCaptionStyle()`関数の定義位置を
  ファイル内で少し上(`updateHighlightBoxVisibility()`より前)に移動した。定義位置が
  変わっただけで、呼び出し側のインターフェースや処理内容は変わっていない。

### 16-3. 対象範囲・互換性

- 変更は `highlight-studio-03-app.html` のみ。警告はあくまで表示のみで、書き出し処理
  (`runBurnAll()`)や実際に焼き込まれる色そのものには一切影響しない(ユーザーが色を
  変えなければ、警告が出ていてもそのまま書き出せる)。
- 既存の文字色・縁取り色・表示位置・折り返し等のロジックには手を入れていない
  (`resolveEffectiveCaptionColors()`への切り出しのみで、計算内容自体は同一)。

### 16-4. 動作確認状況

- インラインJSの構文チェック(`node --check`)通過。IDの重複なし(新規追加した
  `capColorContrastWarning`を含め、全73個のidが重複なしであることを確認)。
- 距離計算ロジックはNode.js上で主要な色の組み合わせについて検算済み(16-1参照)。
- 実際のブラウザ上での警告表示の見た目(色・余白・アニメーション)は、このサンドボックス
  環境では見た目の確認ができていないため、ブラウザで一度、白文字+白縁取りなどの組み合わせを
  選んで表示を確認することを推奨する。

### 16-5. 今後さらに作り込むなら

- 現状の距離判定はRGBユークリッド距離による簡易的なものであり、人間の知覚特性
  (輝度差の寄与が大きいなど)を反映したWCAGのコントラスト比計算ではない。より厳密に
  判定したい場合は、相対輝度ベースのコントラスト比計算に置き換えるとよい。
- 警告は文言のみで、具体的にどちらの色を変えるべきかの提案(自動でおすすめの縁取り色に
  切り替えるボタンなど)までは行っていない。

## 17. 多言語対応(JA/EN切り替え)着手(2026-09-07)

バックログの「4-3. 多言語対応(英語版など)は未着手」に着手した。フルビルドシステムを
持たない静的HTML構成のため、外部ライブラリなしの軽量i18n(`site/i18n.js`)を新規に作り、
主要ページへ組み込んだ。

### やったこと

- `site/i18n.js` を新規作成。仕組みは以下の通り:
  - 各ページで `<script>` により `window.HS_I18N_DICT = { "キー": { ja: "...", en: "..." }, ... }`
    を定義してから `<script src="./i18n.js"></script>` を読み込む。
  - 翻訳したい要素には `data-i18n="キー"`(テキスト置換)、タグを含む場合は
    `data-i18n-html="キー"`(innerHTML置換)、属性を訳したい場合は
    `data-i18n-placeholder` / `data-i18n-title` / `data-i18n-aria-label` などを付与する。
  - ヘッダーの言語切り替えボタンには `data-i18n-lang-toggle` を付けると、現在の言語に応じて
    ラベル(「English」⇔「日本語」)が自動で入れ替わる。
  - 選択した言語は `localStorage`(キー: `hs_lang`)に保存され、ページ遷移後も保持される。
    未対応ページ(`data-i18n`属性が無いページ)には影響しない。
- 以下6ページに `data-i18n` 属性・辞書・言語切り替えボタンを組み込み、日英切り替えに対応した:
  - `highlight-studio-01-landing.html`(トップページ、ヒーロー〜フッターまで全セクション)
  - `highlight-studio-02-auth.html`(ログイン・アカウント作成・メール確認)
  - `highlight-studio-04-reset-password.html`(パスワード再設定の全ステップ)
  - `highlight-studio-06-pricing.html`(料金プラン・FAQ含む)
  - `highlight-studio-10-contact.html`(お問い合わせフォーム)
  - `404.html`
- 各ページ末尾に `node --check` でインラインJSの構文チェック、`data-i18n`属性で使われている
  キーと辞書内のキーの整合性チェック(不足・余剰なし)を実施済み。ID重複も無し。
- ログイン済み判定で動的にテキストを差し替えている箇所(「無料で始める」→「アプリを開く」)は
  スクリプトの実行順序上 `HS_I18N` オブジェクトがまだ読み込まれていないため、
  `localStorage.getItem('hs_lang')` を直接参照する形にして対応した
  (`highlight-studio-01-landing.html` / `highlight-studio-06-pricing.html`)。

### 今回のスコープ外(未対応)

- **`highlight-studio-03-app.html`(メインのエディタ画面)**: ページが最も大きく複雑
  (字幕スタイル、カラーピッカー、警告表示など今回までの拡張がすべて詰まっている)ため、
  今回は着手していない。次にやるなら、まずSTEP1〜3の主要ラベル・ボタンから始めるのが
  現実的。
- **`highlight-studio-05-mypage.html`(マイページ)**: プラン管理・請求情報など項目数が多く、
  今回は未対応。
- **`highlight-studio-07-terms.html` / `08-privacy.html` / `09-tokushoho.html`(利用規約・
  プライバシーポリシー・特定商取引法に基づく表記)**: 法律文書のため、翻訳内容の正確性に
  ついて法務レビューが必要。あえて今回は手を付けず日本語のみのままにしてある。
  ※フッターのリンクラベル自体(「利用規約」など)は他ページ同様に翻訳対象にしてあるが、
  リンク先のページ内容は日本語のまま。
- 対応済み6ページも、100%の網羅ではなく主要な見出し・本文・ボタン・フォームラベルを
  中心に翻訳している。細かい注意書きや稀にしか出ない文言まではカバーしきれていない箇所が
  ある可能性がある。

### 次にやるなら

1. `highlight-studio-03-app.html` と `05-mypage.html` への展開(残り2ページ)
2. 各ページの `HS_I18N_DICT` は現状ページごとに重複定義している(`common.*`キーなど)。
   ページ数が増えるなら、共通辞書ファイル(例: `i18n-common.js`)に切り出すと保守しやすい。
3. 英語版の文言は一度ネイティブ話者やプロの翻訳者によるレビューを通すことを推奨(現状は
   実務上自然な範囲での翻訳に留めている)。
4. 利用規約・プライバシーポリシー・特商法ページの英語版が必要になった場合は、法務確認を
   経てから翻訳に着手すること。

### 追記(2026-09-07): common.*キーの共有ファイル化

上記「2. 今回のスコープ外」の「次にやるなら」2番目に挙げていた、`common.*`キーの重複を
解消する作業を行った。

- `site/i18n-common.js` を新規作成。ナビゲーション(使い方/機能/料金など)、フッター
  (製品/アカウント/サポート各列、コピーライト行のリンクなど)といった、複数ページで
  まったく同じ文言を使うキー(`common.*`、19件)をここに集約した。
  `window.HS_I18N_COMMON = { "common.xxx": { ja: "...", en: "..." }, ... }` という形式。
- `i18n.js` 側を修正し、`HS_I18N_COMMON`(共通)と各ページの `HS_I18N_DICT`(ページ固有)を
  マージしてから使うようにした(`Object.assign({}, HS_I18N_COMMON, HS_I18N_DICT)`。
  キーが重複した場合はページ側の値が優先される)。
- 対応済み6ページすべてで、`<script src="./i18n-common.js"></script>` を
  ページ固有辞書の `<script>` より前に追加し、各ページのローカル辞書からは
  `common.*` キーを削除した(重複定義を解消)。読み込み順序は
  `i18n-common.js` → ページ固有の `HS_I18N_DICT` → `i18n.js` の順。
- 変更後、全6ページで構文チェック(`node --check`)、`data-i18n`属性で参照している
  キーが「共通辞書+ページ辞書」の範囲内で過不足なく揃っているかのチェック、
  スクリプトの読み込み順序チェックを実施し、いずれも問題なし。

これにより、今後ナビゲーションやフッターの文言を変更する際は `i18n-common.js` の1箇所を
直せば全対応ページに反映されるようになった(以前は6ファイルすべてを個別に直す必要が
あった)。`auth.*` / `reset.*` など、ページ固有の「戻る」リンクのようにリンク先が
ページごとに異なるものは、意図的に共通化せずページ側の辞書に残してある。

## 18. favicon一式・メタ情報の整備(2026-09-07)

「企業のサイトと言われても遜色ないように」という依頼を受け、まずビジュアルの完成度
(配色・余白・favicon等の細部)を優先して見直した。

### 確認したこと(既に問題がなかった部分)

- `:root` で定義しているデザイントークン(`--bg` / `--accent` / `--radius` /
  `--shadow-*` など)の値を全11ページ間でプログラム的に突き合わせたところ、
  同じ変数名であれば値は全ページで完全に一致していた(表記ゆれ・コピペミスなし)。
- ヘッダーの基本パディング(`padding:18px 28px`)も全ページで統一されていた。
- OGP画像(`ogp.png`)は既に配色・タイポグラフィともにブランドと一致した仕上がりで、
  修正不要と判断した。

これらの点で、ビジュアルの一貫性という土台は既にできていた。

### 実際に手を入れたこと

1. **favicon一式の追加**。従来は11ページすべてで、ブランドマーク(バーチャート型ロゴ)を
   インラインSVGのdata URIとして`<link rel="icon">`に埋め込んでいるだけだった。
   これはモダンブラウザのタブでは表示されるが、ブックマーク・ホーム画面への追加・
   PWAインストールなど周辺の場面で正しく表示されない、または解像度が粗くなる
   ケースがある。今回、同じブランドマークのデザイン(グレー系バー4本+アクセント色の
   バー1本、角丸の背景付き)をもとに以下を新規生成し、全ページに追加した:
   - `favicon.ico`(16/32/48pxを内包)
   - `favicon-16x16.png` / `favicon-32x32.png`
   - `apple-touch-icon.png`(180×180、iOSホーム画面用)
   - `icon-192.png` / `icon-512.png`(Android/PWAインストール用)
   - `site.webmanifest`(アプリ名・テーマカラー・上記アイコンを参照)
   既存のインラインSVG favicon(モダンブラウザ向け、ベクターで一番綺麗)はそのまま残し、
   これらをフォールバック・補完として追加する形にしてある。
2. **メタ情報の補強**。全ページに `apple-mobile-web-app-title`(ホーム画面での表示名)と
   `og:locale`(`ja_JP`)を追加。さらに多言語対応済みの6ページ
   (`01`, `02`, `04`, `06`, `10`, `404.html`)には `og:locale:alternate`(`en_US`)も
   追加し、英語版があることをSNSシェア時のメタ情報からも分かるようにした。

### 動作確認

- 全11ページで `<html>`/`<head>` タグの対応、既存の `<script>` ブロックの構文
  (`node --check`)に問題がないことを確認済み。
- `wkhtmltoimage`(古いQtWebKitベースのレンダラ)でも一部ページのスクリーンショットを
  撮って目視確認を試みたが、このレンダラは2014年ごろのエンジンで**CSS Gridと
  flexboxのgapプロパティに対応していない**ため、ナビゲーションの余白やヒーロー画像の
  2カラムレイアウトが崩れて表示された。これは実際のモダンブラウザ(Chrome / Safari /
  Edge / Firefoxいずれも対応済み)では発生しない、検証ツール側の制約によるもの。
  実ブラウザでの見た目に影響する変更は行っていない。

### 次にやるなら

- ビジュアル面はほぼ完成しているため、次に手を入れるなら「企業らしさ」の別の軸
  (会社概要ページの追加、コピーの丁寧さの底上げなど、前回選ばなかった選択肢)が候補。
- モダンブラウザでの実際のスクリーンショットが必要な場合は、Playwright等
  Chromiumベースのヘッドレスブラウザを使うこと(`wkhtmltoimage`はCSS Grid非対応のため
  レイアウト確認には不向き)。

## 19. 会社概要ページの追加(2026-09-07)

18章の「次にやるなら」で候補に挙げていた「企業らしさの別の軸」として、会社概要
(About)ページを新規追加した。これまで運営事業者の情報は特商法ページ(09)にしか
無く、それも決済まわりの法定表記という文脈でしか出てこなかったため、サービスの
紹介・ミッション・事業者情報をまとめて見せるページを別途用意した。

### 19-1. 追加したファイル

- `highlight-studio-11-about.html`(新規)。既存の`07-terms.html`と同じCSS変数・
  レイアウト構成(ヘッダー/`.wrap`/`.doc-section`/`spec-table`/シンプルな
  footer-links型フッター)を踏襲した1カラムのドキュメント風ページ。
  - 「1. 私たちが目指しているもの」「2. 大事にしていること(3枚のバリューカード)」
    「3. 運営事業者情報(spec-table)」「4. お問い合わせ導線」の4セクション構成。
  - ミッション文・バリューの文言はランディングページの既存コピー(「動画は一度も
    サーバーに送信されません」等)とトーンを合わせて新規に書き起こした。
  - 事業者情報(商号・代表者・所在地・設立)は09と同様にプレースホルダーのため、
    **公開前に実データへの差し替えが必須**(09と合わせて対応すること)。所在地の
    扱い(開示請求はお問い合わせから)・連絡先メールも09と同じ方針に揃えてある。
  - 多言語対応(JA/EN)は今回のスコープ外。07/08/09と同じく日本語のみ。

### 19-2. 既存ページへの導線追加

- フッターに「会社概要」へのリンクを追加した対象: `01-landing`, `06-pricing`
  (フルフッターの「サポート」列 + 下部のリンク行の両方)、`07-terms`, `08-privacy`,
  `09-tokushoho`, `10-contact`(いずれもフッターのリンク一覧の先頭に追加)。
  `02-auth`/`03-app`/`04-reset-password`/`05-mypage`/`404.html`はもともとフッター
  自体が無いページのため対象外(既存の設計のまま)。
- i18n対応ページ(`01`, `06`, `10`)向けに、`i18n-common.js`へ
  `common.footerAbout: { ja: "会社概要", en: "About Us" }` を追加した。
- `sitemap.xml` に `highlight-studio-11-about.html` のエントリを追加(優先度0.4、
  yearly)。

### 19-3. 動作確認状況

- 追加・変更した全ページで `<div>`の開閉数が一致していることを確認済み。
- `01-landing.html` / `06-pricing.html` / `10-contact.html` のインラインJS
  (`<script>`ブロック)を抽出し `node --check` で構文チェック済み、いずれも問題なし
  (フッターのHTML部分のみの変更で、JSロジック自体には手を入れていない)。
- サイト内の相対リンク(`href="./..."`)を全ページ横断でスキャンし、リンク切れが
  無いことを確認済み(クエリ文字列付きの`02-auth.html?tab=...`系は正常な誤検出)。
- 実ブラウザでの見た目確認(フッターの折り返し、EN表示時のレイアウト)は、この
  サンドボックス環境では確認できていないため、公開前に一度目視推奨。

### 19-4. 今後さらに作り込むなら

- ~~`11-about.html` 自体の多言語対応~~ → 今回(23章)で対応済み。
- ヘッダーのグローバルナビ(01/06などにある「使い方」「機能」「料金」等のリンク列)
  に「会社概要」を含めるかどうかは今回判断が分かれる箇所として見送った(フッターの
  「サポート」列への導線のみとした)。企業サイトとしての回遊性をさらに高めたい場合は
  ヘッダー側への追加も検討の余地がある。
- 事業者情報(19-1)を実データに差し替える際は、09の特商法ページとの記載内容の整合
  (商号・代表者名など)を必ず取ること。

## 20. 事業者情報のプレースホルダーを解消(2026-09-07)

依頼者から「実際にはどこかの企業が使うわけではない(портfolio/デモ用途)」旨の
補足があったため、09・11の両ページに残っていた `(事業者名を記載)` 等の未入力
プレースホルダーを、架空だが具体的な会社名・代表者名に差し替えた。プレースホルダーの
文字列がそのまま画面に出ている状態は「企業のサイトとして遜色ない見た目」から
もっとも遠い部分だったため、今回優先して対応した。

### 20-1. 変更内容

- `highlight-studio-09-tokushoho.html` / `highlight-studio-11-about.html` の
  事業者情報テーブルを以下の架空データで統一(2ページ間で内容を一致させてある):
  - 商号・販売事業者: 株式会社ハイライトスタジオ
  - 運営統括責任者・代表者: 代表取締役 佐藤 直哉
  - 設立(11のみ): 2023年4月
  - 所在地: 従来通り「開示請求があれば開示」方式のまま(小規模事業者の一般的な
    運用として、これ自体は自然な記載のため変更していない)。
- 両ページにあった大きめの警告ボックス(note-box、「サンプルです、公開前に
  差し替えてください」)は、実際の公開を前提としない今回の用途にはそぐわない
  (かえって「未完成に見える」)ため撤去し、代わりにテーブル下に小さく
  「※本ページに記載の会社名・代表者名はデモンストレーション用の架空のものです。」
  という控えめな注記のみを残した。ページの見た目としては通常の企業サイトの
  会社情報ページに近い印象になっている。
- `01-landing.html` / `06-pricing.html` のフッター著作権表示を
  `© 2026 Highlight Studio` から `© 2026 株式会社ハイライトスタジオ` に変更
  (英語表示時は `© 2026 Highlight Studio, Inc.` に切り替わるよう
  `i18n-common.js` に `common.footerCopyright` キーを追加し、
  `data-i18n="common.footerCopyright"` で紐付けた)。
- `09-tokushoho.html` の「最終更新日」表記を本日の日付に更新。

### 20-2. 対象範囲・互換性

- 変更は `09-tokushoho.html` / `11-about.html` / `01-landing.html` /
  `06-pricing.html` / `i18n-common.js` のみ。他ページへの影響なし。
- 決済・返金・提供時期など、09ページのそれ以外の記載内容(価格・支払い方法等)は
  変更していない。

### 20-3. 動作確認状況

- 変更対象の全ファイルで`<div>`の開閉数一致を確認。
- `01`/`06`/`10`/`404`/`02`/`04`(i18n対応ページ全て)について、
  `data-i18n`系属性で参照しているキーが「共通辞書+ページ辞書」の範囲内で
  過不足なく揃っていることをスクリプトで再確認済み(不足キー無し)。
- サイト内の相対リンクを全ページ横断で再スキャンし、リンク切れが無いことを確認済み。

### 20-4. 今後さらに作り込むなら

- 架空データ(株式会社ハイライトスタジオ / 佐藤 直哉)は、他の書類(利用規約や
  プライバシーポリシーの文中など)で事業者名を名指ししている箇所が今後増える場合は
  同じ名称に統一すること。
- 完全に実在の企業として運用する予定ができた場合は、20-1で入れた架空データを
  実データに差し替え、テーブル下の「デモンストレーション用の架空のもの」という
  注記も削除すること。

## 21. アプリ本体ページのタイトル修正・OGP追加、実ブラウザでの見た目確認(2026-09-07)

これまでの回では確認できていなかった「実際のブラウザでの見た目」を、今回はじめて
Playwright(Chromiumヘッドレス)でスクリーンショットを撮って確認できた。あわせて、
確認の過程で見つかった開発中の名残りを1件修正した。

### 21-1. 見つかった問題と修正

- `highlight-studio-03-app.html` のブラウザタブタイトルが `Highlight Studio — 統合版`
  になっていた。「統合版」は開発中の内部呼称がそのまま残っていたもので、ユーザー向け
  としては不自然だったため `Highlight Studio — エディタ` に変更した。
- 同ページには `01`/`06`/`09`/`11` など他の主要ページにある `og:type` / `og:title` /
  `og:description` / `og:image` / `og:url` / `canonical` が一つも設定されていなかった
  ため、他ページと同じ形式で追加した(SNSでリンクを共有した際のプレビュー表示や、
  検索エンジンでの正規URL指定に対応)。
- `sitemap.xml` には元々このページのエントリが既にあったため、変更なし。

### 21-2. 実ブラウザでの見た目確認(今回はじめて実施)

- これまでの回では `wkhtmltoimage`(CSS Grid非対応の古いレンダラ)でしか確認できて
  いなかったが、今回はこの環境に `playwright` がインストール済みであることを確認し、
  Chromiumヘッドレスでの実際のスクリーンショットで以下を確認した:
  - デスクトップ幅(1280px): トップページ、会社概要、特商法表記、料金プラン、
    アプリ本体(STEP1画面)、ログイン画面
  - モバイル幅(390px): トップページ、会社概要(ヘッダーの折り返し、バリューカードの
    1カラム化、テーブルのラベル/値の縦積みを含む)
  - いずれも崩れ・はみ出し・意図しない重なりは見られず、デザイントークンの統一感も
    含めて想定通りの見た目だった。
- **今後、この環境で見た目を確認したい場合は `playwright` の Chromium を使うこと**
  (18章で使った `wkhtmltoimage` はCSS Gridに対応しておらずレイアウトが崩れて見える
  ため、確認用途には不向き)。

### 21-3. 対象範囲・互換性

- 変更は `highlight-studio-03-app.html` の `<head>` 内(タイトル・メタタグ)のみ。
  画面表示・JS・機能には一切変更なし。
- 変更後、`<div>`の開閉数一致・インラインJSの`node --check`を再確認済み、問題なし。

## 22. マイページのプラン表示の不整合バグを修正(2026-09-07)

21章で導入したPlaywrightでの実ブラウザ確認を、今回はログイン状態(localStorageに
`hs_loggedIn`等をセット)でも行い、`highlight-studio-05-mypage.html` で見た目の
バグを1件発見・修正した。

### 22-1. 見つかったバグ

- マイページ上部のアカウントカード内にあるプラン表示バッジ(`.meta-pill`、
  「無料プラン」「書き出し ◯件」の左側の丸いラベル)が、id を持たない静的HTMLの
  ままで、ページ読み込み時のJS処理から一切更新されていなかった。
- そのため、実際の契約プラン(`localStorage`の`hs_plan`)がスタンダードやプロで
  あっても、このバッジだけ常に「無料プラン」と表示され続けていた。ヘッダー右上の
  プランバッジ(`headerPlanPill`)やページ中央のプランカード(`planName`)は正しく
  更新されていたため、**同じ画面内でプラン表示が一致していない**状態になっていた。
- 通常のデモ操作(ログイン→アプリを使う、程度の導線)ではこの箇所を直接
  見比べる機会が少なく、これまでの回では気づかれていなかった。今回、
  Playwrightで `hs_plan='standard'` を仮設定してマイページを実際にレンダリングして
  はじめて発見できた。

### 22-2. 修正内容

- 対象の`<span>`に`id="accountPlanPill"`を付与。
- プラン情報を反映する既存の処理(`headerPlanPill`を設定している箇所のすぐ下)に
  `document.getElementById('accountPlanPill').textContent = plan.name;`を追加し、
  他の2箇所(ヘッダー・プランカード)と同じ`plan.name`を表示するようにした。
- 変更は `highlight-studio-05-mypage.html` の1ファイルのみ、追加2行(id付与1箇所+
  JS1行)。ロジックの分岐や他の表示には影響しない。

### 22-3. 動作確認状況

- 修正後、Playwrightで`hs_plan='standard'`を設定した状態でマイページを再読み込みし、
  アカウントカードのバッジ・プランカードの名称・ヘッダーのバッジの3箇所すべてが
  「スタンダード」に揃って表示されることをスクリーンショットで確認済み。
- インラインJSの`node --check`、`<div>`開閉数の一致、id重複なしを再確認済み。

### 22-4. 今後さらに確認するなら

- 今回は`standard`のケースのみ実ブラウザで確認した。`pro`・`admin`プランでも
  同様に3箇所が揃って表示されることは、ロジック上は同じ`plan.name`を参照している
  ため揃うはずだが、実際の画面確認はしていない。念のため公開前に一通り確認すると
  なお安心。
- 今回のように「同じ情報を画面内の複数箇所に別々に表示している」箇所が他にも
  無いか(例: 料金プランページの表示中プランなど)、時間があれば洗い出すと
  さらに品質を上げられる。

## 23. 会社概要ページの多言語対応 + プラン表示バグの追加確認(2026-09-07・4回目)

### 23-1. プラン表示バグ(22章)の追加確認

22章で修正した「マイページのアカウントカード内のプランバッジ(`accountPlanPill`)が
常に『無料プラン』のまま食い違っていた」バグについて、22-4で「pro・adminプランでも
揃って表示されるはずだが未確認」としていた点を、実際にPlaywrightでレンダリングして
確認した。

- `hs_plan` を `standard` / `pro` / `admin` それぞれに設定した状態でマイページを開き、
  ヘッダーのバッジ(`headerPlanPill`)・プランカードの名称(`planName`)・アカウントカードの
  バッジ(`accountPlanPill`)の3箇所を取得したところ、いずれのプランでも3箇所とも
  同じプラン名で揃って表示されることを確認した(例: `standard`→3箇所とも「スタンダード」、
  `admin`→3箇所とも「管理者(デバッグ用)」)。**バグの再発・見落としはなし。**
- あわせてモバイル幅(390px)でも同様に確認し、`admin`プランのようにプラン名が長い
  場合でもヘッダーのバッジがはみ出す・折り返して崩れるといった問題がないことを
  スクリーンショットで確認した。
- 「同じ情報を画面内の複数箇所に別々に表示している箇所が他にもないか」(22-4で
  挙げていたもう一つの懸念)についても、料金プランページ(`06-pricing.html`)や
  アプリ本体(`03-app.html`)のプラン関連表示を確認したが、mypage以外にはプラン名を
  複数箇所に重複表示している箇所は見当たらなかった。

対象範囲: 確認のみで、コードの変更はなし。

### 23-2. 会社概要ページ(`11-about.html`)の多言語対応

17章・19-4で「今後さらに作り込むなら」として残っていた、`11-about.html`自体の
JA/EN切り替え対応を行った。他の6ページ(`01`, `02`, `04`, `06`, `10`, `404.html`)と
同じ`i18n.js` / `i18n-common.js`の仕組みをそのまま踏襲している。

#### 対応した内容

- ヘッダーの「トップに戻る」リンク、言語切り替えボタン(`lang-toggle`)を追加。
- 本文(ミッション・バリュー3枚・お問い合わせ導線・最終更新日ラベル)を
  `data-i18n`属性で翻訳対象にし、ページ固有の辞書(`about.*`、22キー)を追加した。
- フッターのリンク(会社概要/利用規約/プライバシー/特商法/お問い合わせ)は、
  他ページで既に定義済みの共通キー(`common.footerAbout`等)をそのまま再利用した
  (辞書の重複追加なし)。
- **事業者情報テーブル(商号・代表者・所在地・設立・事業内容・連絡先の値そのもの)は、
  07〜09ページ(利用規約・プライバシー・特商法)と同じ方針であえて翻訳対象外にし、
  日本語のまま残してある。** 項目名(「商号」→「Company name」等、`<th>`側)のみ
  翻訳し、値(`<td>`側の実データ)は法律文書に近い内容のため今回も日本語のみとした。
  末尾の注記文(「※本ページに記載の会社名・代表者名はデモンストレーション用の
  架空のものです」)はタグを含むため`data-i18n-html`で対応。

#### 対象範囲・互換性

- 変更は `highlight-studio-11-about.html` のみ。他ページへの影響なし
  (共通辞書`i18n-common.js`は参照のみで、キーの追加・変更は行っていない)。
- 言語を切り替えなければ(初期状態=日本語)見た目・文言は従来と完全に同一。

#### 動作確認状況

- インラインJS(辞書定義部分)を抽出し `node --check` で構文チェック済み、問題なし。
- `<div>`の開閉数一致、id重複なしを確認済み。
- `data-i18n` / `data-i18n-html`属性で参照しているキーが、共通辞書+ページ辞書の
  範囲内で過不足なく揃っていることをスクリプトで確認済み(不足・余剰キーなし)。
- Playwright(Chromiumヘッドレス)で、日本語表示(デスクトップ)・英語表示
  (デスクトップ・モバイル390px)の3パターンを実際にスクリーンショットで確認し、
  レイアウト崩れ・はみ出しがないことを確認済み。

### 23-3. 今後さらに作り込むなら

- 事業者情報テーブルの値(会社名・代表者名など)を英語版でも表示したい場合は、
  法務確認のうえで英語表記を用意し、`about.companyName`等のキーに`en`値を追加すること
  (現状はテーブルの項目名のみ翻訳済みで、値は日本語のまま)。
- これで多言語対応済みのページは7ページ(`01`, `02`, `04`, `06`, `10`, `11`, `404.html`)
  になった。残る`03-app.html`(最も複雑)・`05-mypage.html`への展開が引き続き
  次の候補。

## 24. マイページ(`05-mypage.html`)の多言語対応 + 未発見バグの修正(2026-09-08)

23-3で「次の候補」としていた`05-mypage.html`のJA/EN対応に着手した。このページは
アカウント情報・プラン・利用状況・書き出し履歴・4種類のモーダル(お支払い方法/
プロフィール編集/書き出し削除/アカウント削除)・トースト通知など、これまで
対応した7ページの中で最もJSによる動的なテキスト書き換えが多いページだった。

### 24-1. 対応した内容

- 他の7ページと同じ`i18n.js` / `i18n-common.js`の仕組みを踏襲し、ヘッダーに
  言語切り替えボタンを追加。静的なテキスト(見出し・ボタン・モーダルの説明文・
  フィールドラベル・placeholderなど)に`data-i18n`系属性を付与し、`mypage.*`
  辞書(84キー)を追加した。
- このページ特有の課題として、多くのテキストが**JSによって動的に書き込まれる**
  (プラン名・価格・説明文、利用状況の残数表示、トースト通知、バリデーション
  エラー文、削除確認ダイアログの文面など)。これらは`data-i18n`属性では対応できないため、
  `T(key, vars)`という小さなヘルパー関数(`HS_I18N.t()`のラッパー、`{n}`等の
  プレースホルダー置換に対応)を追加し、該当箇所のJS内リテラル文字列をすべて
  この関数経由に置き換えた。
- `document.addEventListener('hs:langchange', ...)`で、言語切り替え時に
  アカウント表示・プランカード・書き出し履歴(件数表示部分)を再描画するように
  し、画面を開き直さなくても言語切り替えボタン1つで即座に表示が切り替わるように
  した。
- プランの内部データ(`PLANS`オブジェクト)は、名称・価格・説明文を直接持つ形から
  「プランキー(`free`/`standard`/`pro`/`admin`)から翻訳辞書のキーを組み立てて
  都度`T()`で引く」形に変更した(`getPlan(key)`関数)。
- なお、書き出し履歴のデモデータ(クリップのタイトル・字幕スタイル名)自体は、
  11章の会社概要ページの事業者情報と同じ考え方で**翻訳対象外とし、日本語のまま**
  残した(サンプルコンテンツの中身であり、UI文言ではないため)。

### 24-2. 見つけて直したバグ(「よりよくする」ため追加で確認した範囲)

- 「アカウントを削除する」ボタンを押すと、確認モーダルが開かず、コンソールに
  `ReferenceError: userName is not defined`が発生し**退会フローがまったく機能して
  いなかった**バグを発見・修正した(未定義のグローバル変数`userName`を参照していた
  箇所を、`localStorage`から表示名を取得する処理に置き換え)。
- これは今回のi18n対応中に、Playwrightで一通りのモーダルを開いて動作確認した際に
  偶然見つかったもので、多言語対応そのものとは無関係の既存バグ(今回の変更を
  加える前の`highlight-studio-with-admin.zip`の時点で既に存在していたことを確認済み)。
  影響範囲は「アカウント削除」導線のみで、他の機能への影響はない。

### 24-3. 動作確認状況

- 追加・変更したインラインJS(dict定義部分含む)を`node --check`で構文チェック済み。
- `<div>`の開閉数一致(64/64)を確認済み。
- `data-i18n`系属性で参照しているキーが辞書内に過不足なく揃っていることをスクリプトで
  確認済み(不足キーなし)。
- Playwright(Chromiumヘッドレス)で、standard/pro/adminの各プランについて日本語・
  英語表示を実際にレンダリングし、以下を確認:
  - アカウントカード・プランカード・利用状況・書き出し履歴・アカウント設定の
    各セクションが崩れなく表示されること。
  - お支払い方法/プロフィール編集/書き出し削除/アカウント削除の全4モーダルが
    それぞれの言語で正しく開き、修正後は「アカウント削除」モーダルもエラーなく
    開いて内容(ログイン情報・現在のプラン・書き出し件数)が正しく表示されること。
  - モバイル幅(390px)でも、`admin`プランのように文字数の多いプラン名を含めて
    レイアウト崩れがないこと。
  - 言語切り替えボタンを押した際、ページを開き直さずにプランカード・利用状況・
    アカウント表示が即座に切り替わること。

### 24-4. 対象範囲・互換性

- 変更は`highlight-studio-05-mypage.html`の1ファイルのみ。他ページへの影響なし
  (共通辞書`i18n-common.js`は参照のみで、キーの追加・変更は行っていない)。
- 言語を切り替えなければ(初期状態=日本語)、バグ修正箇所を除き見た目・文言は
  従来と同一。バグ修正箇所(アカウント削除モーダル)は、修正前は機能しておらず
  常にエラーで止まっていたため、動作するようになったこと自体が変更点。

### 24-5. 今後さらに作り込むなら

- これで多言語対応済みのページは8ページ(`01`, `02`, `04`, `05`, `06`, `10`, `11`,
  `404.html`)になった。残る`03-app.html`(最も複雑・音声認識/字幕焼き込みUIを含む)
  への展開が引き続き次の候補。
- 今回のように「動的にテキストを書き込むJSの中に、辞書化されていないリテラル文字列が
  残っていないか」は、`03-app.html`のような複雑なページほど見落としやすいため、
  着手時はページ全体のJSを一通り洗い出すことを推奨する。

## 25. 精度検証パス — バックエンドの重大バグ発見・修正(2026-09-08・追加分)

前章(24)のマイページ対応に続けて、「精度を上げる」目的で全体の横断検証を行った。

### 25-1. フロントエンド8ページ(日英対応済み分)の検証 — 問題なし

- 各ページの翻訳辞書(`HS_I18N_DICT`)を全ファイル分抽出し、以下をスクリプトで
  機械的に検証: (1) `{n}`等のプレースホルダーが日本語版・英語版で過不足なく
  一致しているか、(2) 埋め込みリンクの`href`が日本語版・英語版で一致しているか、
  (3) 頻出語(ログイン/書き出し/無料プラン/アップグレード/送信/キャンセル等)の
  訳語がページをまたいで統一されているか。いずれも問題は見つからなかった。
- Playwrightで実際にフォーム送信(サインアップの未入力バリデーション)・
  パスワード再設定・料金ページのFAQ開閉(4件)・お問い合わせフォーム送信を
  操作し、コンソールエラーが出ないこと、バリデーションメッセージが正しく
  表示されることを確認した。

### 25-2. バックエンド(Flask叩き台)の検証 — バグを発見・修正

同梱のテストスイート(`pytest`)を実際に実行したところ、15件中2件が失敗して
いることが分かった。

- **症状**: 「確認メールのリンクからのメール確認」「パスワード再設定」の
  どちらも、`TypeError: can't compare offset-naive and offset-aware datetimes`
  という例外で処理が止まっていた。
- **原因**: トークンの有効期限(`expires_at`)を保存する際はタイムゾーン情報
  付きの日時(UTC)を使っていたが、SQLite(このプロトタイプが使うDB)は
  タイムゾーン情報を保持しないため、DBから読み戻した時点でタイムゾーン情報が
  失われた日時になる。これを、タイムゾーン情報付きの「現在時刻」と直接
  比較しようとしていたため、Python側で比較不能として例外になっていた。
- **対応**: `app/routes/auth.py`に、タイムゾーン情報のない日時をUTCとして
  補正する小さなヘルパー関数`_as_aware_utc()`を追加し、該当2箇所の比較処理を
  この関数経由に変更。
- **結果**: `pytest tests/`が15件中15件合格に変化(修正前: 13件合格・2件失敗)。
- **影響範囲**: `verify-email`・`reset-password`の2エンドポイントのみ。他の
  エンドポイント・フロントエンドへの影響はない。

### 25-3. 見送ったこと — アプリ本体(`03-app.html`)の多言語対応

24章の最後で次候補としていた`03-app.html`(2166行、音声認識・ffmpeg処理を含む
最も複雑なページ)の英語対応は、今回は着手を見送った。

このページの`<script type="module">`は、外部CDN(`cdn.jsdelivr.net`)から
音声認識ライブラリを読み込む構成になっており、今回の検証環境ではこの
CDNへのネットワークアクセスが許可されていなかったため、実際のボタン操作・
UI連動(スタイル選択でハイライトボックス欄が表示される、等)を確認する
ことができなかった(静的な構造チェック — タグの対応・ID重複の有無 — は
問題なし)。実際の動作確認ができない状態で2000行超の改修に踏み込むのは
差し戻しリスクが高いと判断し、次回、動作確認ができる環境で着手することを
推奨する。

### 25-4. 対象範囲

- 変更は`backend/app/routes/auth.py`の1ファイルのみ(関数追加+2箇所の修正)。
- フロントエンドはこの章では変更なし(24章時点のまま)。

## 26. 精度検証パス・第2弾(2026-09-08・追加分)

25章の続きとして、さらに範囲を広げて確認した。**今回の範囲では新たな不具合は
見つからなかった**(コード変更なし)。確認した内容を記録として残す。

### 26-1. バックエンドの全ルート・モデルを再読了

`auth.py`(25章で修正済みの箇所を含め全体)・`billing.py`(Stripe連携)・
`contact.py`・`models.py`・`config.py`・`__init__.py`(管理者アカウント自動作成
ロジック含む)・`email_service.py`をすべて読み直した。ロジック・エラーハンドリング
・セキュリティ配慮(パスワードリセットでのユーザー列挙攻撃対策等)に問題は
見当たらなかった。

### 26-2. 利用規約・プライバシーポリシー・特定商取引法ページとの整合性確認

これまでのセッションで料金プランや機能の有無(顔カメラズーム機能の削除、
字幕スタイル・音声認識モデルのプラン限定化など)を修正してきたが、一度も
手を入れていない`07-terms.html`・`08-privacy.html`・`09-tokushoho.html`の
3ページが、その修正と矛盾する記述を残していないかを確認した。具体的な本数・
料金等の数値は記載されておらず、「動画データはサーバーに送信されない」という
アーキテクチャ上の説明も他ページと一致していることを確認した。矛盾なし。

### 26-3. サイト全体のリンク切れ・辞書の重複キーチェック

- 全12ページの`href`/`src`(相対パス)がすべて実在するファイルを指していることを
  スクリプトで確認(切れているリンクなし)。
- 各ページの翻訳辞書内に、同じキーが二重定義されて後勝ちで意図せず上書きされて
  いる箇所がないことを確認(重複なし)。

### 26-4. まとめ

25章のバックエンド修正が今回発見できた唯一の実害のある不具合であり、それ以外の
部分(フロントエンド8ページ・バックエンド全体・法務ページ)は、今回確認した
範囲では健全な状態と判断する。次にリスクが残っているのは、動作確認ができて
いない`03-app.html`のみ(25-3参照)。

## 27. バックエンドを実際に起動しての検証 — `.env`読み込み漏れの発見・修正(2026-09-08)

25章・26章までの検証は、環境のネットワーク制限のため`pip install`ができず、
`python -m py_compile`による構文チェックと、コードを目で読む静的レビューに
留まっていた(README・6-1-1に明記の通り)。今回はじめて`pip install -r
requirements.txt`が通る環境で作業できたため、実際にインストールして
起動・pytest実行まで行った。

### 27-1. 見つけた不具合: `.env`が一切読み込まれていなかった

- **症状**: README・本ファイル「6-1. バックエンド叩き台を追加」の手順通り
  `cp .env.example .env`で`.env`を作成し値を埋めても、`python run.py`で
  起動した際にその値が一切反映されない(`SECRET_KEY`・`JWT_SECRET_KEY`・
  Stripe/SendGridの各キー・事業者情報・デバッグ管理者アカウントの設定など、
  `.env`に書く項目すべてが対象)。
- **原因**: `requirements.txt`に`python-dotenv`が入っているにもかかわらず、
  `app/config.py`を含むどのファイルからも`load_dotenv()`が一度も呼ばれて
  いなかった。`os.environ.get(...)`はOSの環境変数(shellの`export`や
  Dockerの`ENV`など)しか見ないため、ファイルとして置いた`.env`の中身は
  Pythonプロセスからは見えないままだった。
- **気づいた経緯**: 今回はじめて`pytest`を実際に実行できたため、
  `JWT_SECRET_KEY`が期待通りの値になっているかを確認する過程で発覚。
  これまでの構文チェックだけのレビューでは検出できない種類の不具合だった。
- **修正**: `app/config.py`の先頭(`BaseConfig`クラス定義より前)で
  `load_dotenv()`を呼ぶように変更。`override=False`がデフォルトなので、
  本番のコンテナ/PaaS環境などですでにOS環境変数が設定されている場合は
  そちらが優先され、`.env`の値で上書きされることはない。

### 27-2. 連鎖して見つけた2つ目の不具合: 空文字列がデフォルト値を潰す

上記27-1を修正して`.env`が実際に読み込まれるようにしたところ、今度は
`pytest`が別の理由で失敗するようになった。

- **症状**: `.env.example`(および、それをコピーしただけでまだ値を
  埋めていない`.env`)は各キーを`SECRET_KEY=`のように「キー名だけ・値は
  空文字列」の形で列挙している。`os.environ.get("SECRET_KEY", "change-me-in-.env")`
  という書き方は「キーが存在しない」場合しかデフォルト値を使わないため、
  `.env`経由でキーが(空文字列として)存在すると、意図していた
  デフォルト値("change-me-in-.env"など)ではなく空文字列がそのまま
  採用されてしまい、`JWT_SECRET_KEY`が空になってJWT発行時に例外が発生した。
- **修正**: `SECRET_KEY` / `JWT_SECRET_KEY` / `DATABASE_URL` /
  `JWT_ACCESS_TOKEN_EXPIRES_MIN` / `ALLOWED_ORIGINS` / `MAIL_FROM_ADDRESS`
  (=「未設定なら意味のあるデフォルト値を使いたい」項目)を、
  `os.environ.get(key, default)`から`os.environ.get(key) or default`
  (空文字列もFalsyとして扱いdefaultにフォールバックする書き方)に変更。
  `STRIPE_*`・`SENDGRID_API_KEY`・`BUSINESS_*`・`SITE_DOMAIN`・
  `ADMIN_EMAIL`・`ADMIN_PASSWORD`はもともとデフォルトが空文字列のため
  この問題の影響を受けず、変更していない。

### 27-3. 追加した安全装置: 本番環境でのプレースホルダー秘密鍵チェック

27-1の不具合の後始末として、「`.env`を埋め忘れたまま本番公開してしまう」
事故を防ぐガードを`app/__init__.py`に追加した。

- `create_app()`内で、選択された設定が`ProductionConfig`のときだけ
  `SECRET_KEY` / `JWT_SECRET_KEY`がデフォルトのプレースホルダー値
  (`"change-me-in-.env"`)のままになっていないかを確認し、
  なっていれば起動時に`RuntimeError`で明示的に止める
  (`_reject_insecure_production_secrets()`)。
- 開発・テスト環境(`development` / `testing`)は対象外で、これまで通り
  プレースホルダーのままでも起動できる(挙動を変えていない)。

### 27-4. 実際に`pip install && pytest`を実行して検証

- `python -m venv`で仮想環境を作成し、`requirements.txt`を実インストール
  (Stripe/SendGridパッケージ含め、依存関係の衝突なくインストール成功)。
- `pytest tests/`を実行し、既存15件 + 今回追加4件(下記27-5)の
  **計19件すべて合格**を確認。これまでの7回のレビューでは一度も
  実行できていなかった、初めての実行結果。
- 上記とは別に、`.env`に実際に値を書いたファイルを用意して
  `app.config.BaseConfig.SECRET_KEY`がその値になることを確認するテストも
  追加し、27-1の修正が実際に機能していることを検証した。

### 27-5. 追加したテスト(`tests/test_config_safety.py`)

- `.env`(またはOS環境変数)で秘密鍵を設定していない状態で本番設定
  (`create_app("production")`)を起動しようとすると`RuntimeError`になること。
- 十分な秘密鍵を設定していれば本番設定でも問題なく起動できること。
- 開発設定ではプレースホルダーのままでも従来通り起動できること
  (今回のガードが本番設定のときだけ働くことの確認)。
- `.env`ファイルに書いた値が実際に`BaseConfig`へ反映されること
  (27-1の修正そのものの回帰テスト)。

これらのテストは、設定クラスの属性が**モジュール読み込み時に一度だけ**
`os.environ`を読んで決まる(実行時に動的に読み直されるわけではない)という
`config.py`の作りに合わせて、`pytest`のmonkeypatchで直接環境変数を
書き換えるのではなく、別プロセスを起動して確認する方式にしてある
(実運用でも環境変数は必ずプロセス起動前に確定しているため、この方式の
ほうが実際の使われ方に近い)。

### 27-6. 対象範囲・影響

- 変更ファイル: `backend/app/config.py`(`load_dotenv()`追加 + デフォルト値の
  フォールバック方式修正)、`backend/app/__init__.py`(本番安全装置追加)、
  `backend/README.md`(手順の実態に合わせて更新)、
  `backend/tests/test_config_safety.py`(新規)。
- フロントエンド(`frontend/`配下)は今回変更していない。
- 影響は「`.env`の値が実際に効くかどうか」と「本番起動時の安全確認」のみで、
  各APIエンドポイント自体のロジック(25章で修正した認証まわり含む)は
  変更していない。

### 27-7. 残っている課題(変わらず)

- `03-app.html`(音声認識・ffmpeg処理を含む最も複雑なページ)の英語対応は、
  今回も外部CDN(`cdn.jsdelivr.net`)への検証環境からのアクセスができず、
  実際のボタン操作の動作確認ができないため見送った(25-3から状況は変わらず)。
- バックエンドは今回はじめて実インストール・pytest実行ができたが、
  実際のStripe/SendGridキーを使った本物の決済・メール送信の疎通確認は
  まだ行っていない(引き続き要検証)。

## 28. `03-app.html`をCDNモック経由で初めて実ブラウザ実行検証(2026-09-08・2件目)

25-3・27-7で繰り返し「外部CDN(`cdn.jsdelivr.net`)への検証環境からのアクセスが
できないため、`03-app.html`(音声認識・ffmpeg処理を含む最も複雑なページ)の
実際のボタン操作の動作確認ができない」としていた制約について、CDNへの実際の
アクセスを迂回する方法を見つけて検証した。

### 28-1. やったこと

- Playwright(Chromiumヘッドレス)の`page.route()`機能で、`03-app.html`が
  読み込む`https://cdn.jsdelivr.net/npm/@huggingface/transformers@3.0.0`への
  リクエストをインターセプトし、実際のライブラリの代わりに`pipeline`/`env`の
  最小限のダミー実装(スタブ)をその場で返すようにした。これにより、実際に
  そのCDNへ通信することなく、ESモジュールの読み込み自体を成立させ、
  ページのJSを最後まで実行できる状態を作った。
- ローカルの`http.server`で`frontend`フォルダを配信し、上記のスタブを
  仕込んだ状態で`03-app.html`を実際にブラウザで開き、以下を操作・確認した:
  - `hs_loggedIn`未設定だとログインページへリダイレクトされる仕様を確認した
    うえで、`localStorage`に`hs_loggedIn=1`等を設定してアプリ本体に入れる
    ようにした。
  - スタイル選択(`styleSegmented`)の2つのボタンをそれぞれクリックし、
    「イラスト+テロップ型」を選んだときだけ`highlightBoxField`(ハイライト
    ボックスのテキスト欄)が表示され、もう一方では表示されないことを実際の
    DOM状態で確認した(想定通りの動作)。
  - ダミーの動画ファイル(空データ・.mp4拡張子)を`videoInput`に選択させ、
    「盛り上がりを検出する」ボタン(`toStep2Btn`)が有効化されることを確認した。
  - 上記の操作を通じて、コンソールエラー・JS例外(`pageerror`)が
    発生しないことを確認した(発生したのは、意図的にブロックしたGoogle
    Fonts/unpkgへのリクエスト失敗のみで、アプリのロジックに起因するものはなし)。
- あわせて、`<div>`タグの開閉数一致(107/107)・id重複なしをスクリプトで再確認、
  サイト全体12ページの内部リンクにも新たな問題がないことを再確認した。

### 28-2. 結果

**このページのJSロジック自体に不具合は見つからなかった。** これまで「静的な
構造チェックしかできていない」としていた部分について、今回初めて実際の
ブラウザ実行による動作確認ができ、少なくとも検証した範囲(ステップ1の
UI操作)は正常に動作することを確認できた。

### 28-3. 引き継いだツール: `tools/verify_app_html.py`

上記の検証方法を、次の担当者(人・AIどちらでも)がそのまま再利用できるように
`tools/verify_app_html.py`としてこのZIPに同梱した。使い方はスクリプト冒頭の
docstringに記載。実行には`pip install playwright && playwright install
chromium`が別途必要(このスクリプトは手動検証用のツールであり、
`backend/requirements.txt`やpytestスイートには含めていない)。

このスクリプトの仕組み(CDNリクエストをスタブでインターセプトする)を使えば、
今後`03-app.html`に手を入れる際(特に24章・25-3で次候補としていた多言語対応)、
実際にボタン操作を伴う動作確認をしながら安全に進められる。

### 28-4. 残っている制約(引き続き要注意)

- 今回スタブ化したのはあくまで`pipeline`/`env`の最小限のダミーで、
  **実際の音声認識・ffmpeg変換そのものの精度や処理時間は検証できていない**。
  それらの確認には、実ネットワークでモデル・ffmpeg.wasmをダウンロードできる
  環境が別途必要(6-1・25-3から状況は変わらず)。
- 今回確認したのはステップ1(取込・設定)のUI操作のみ。ステップ2〜4
  (検出実行・字幕編集・書き出し)は、ダミー動画ファイルでは実際の音声・
  映像データがないため、`<video>`要素のデコードに依存する処理の手前で
  止まってしまい、今回は検証範囲に含めていない。本物の動画ファイルを使った
  E2E検証は今後の課題として引き継ぐ。
- `03-app.html`の多言語対応(24-5・25-3から繰り返し次候補としている)は
  今回も着手していない。ただし28-3のツールにより、着手時の安全網は
  今回で整った。

### 28-5. 対象範囲

- 新規追加ファイルは`tools/verify_app_html.py`のみ。フロントエンド・
  バックエンドの既存ファイルへの変更はこの章ではなし(前章27の変更のみ有効)。

## 29. 公開前 最終チェック(2026-09-08・11回目)

「公開しても大丈夫か」の最終確認として、これまでの回とは独立に全体を
横断的に再検証した。**新たに1件、実際にユーザーへ影響するバグを発見・
修正した。** それ以外の範囲(リンク・辞書・バックエンドの修正内容)は
健全な状態を再確認できた。

### 29-1. 見つけて直したバグ: モバイル幅での横スクロール(`01-landing.html`)

- **症状**: スマートフォン相当の画面幅(390px)で`highlight-studio-01-
  landing.html`を開くと、ページ全体がわずかに横スクロールできてしまう
  (`scrollWidth`が`clientWidth`より約65px大きい)状態だった。これまでの
  回のモバイル確認(18章・21章など)は主要セクションの見た目のスクリーン
  ショット比較が中心で、「横スクロール自体が発生していないか」を機械的に
  検出してはいなかったため、10回のレビューを通じて見つかっていなかった。
- **原因**: 最終CTAセクション(`.final-cta`)の背景装飾(`::before`、
  黄色みのグラデーションの光彩)が幅520pxの固定サイズで中央寄せされており、
  390px幅の画面ではその装飾が左右にはみ出す。`.final-cta`側に`overflow:
  hidden`が指定されていなかったため、この非表示であるべきはみ出し分が
  ページ全体の横スクロール領域として実際に発生していた(装飾自体は
  `pointer-events:none`・`z-index:-1`で見た目には気づきにくい)。
- **修正**: `.final-cta`に`overflow:hidden`を追加(1行のみ)。装飾は
  ぼかしのかかったグラデーションのため、はみ出し部分を切り取っても
  見た目への影響は実質なく、意図していた中央の光彩表現はそのまま残る。
- **確認方法**: Playwrightで全12ページ×2幅(デスクトップ1280px/モバイル
  390px)を機械的にレンダリングし、`document.documentElement.scrollWidth
  > clientWidth`を全ページで検査するスクリプトを新規に用意して実行。
  修正前は`01-landing.html`のモバイル幅のみで検出、修正後は全22パターン
  (12ページ×2幅、一部ログイン状態など)で横スクロールなし・JSエラーなしを
  確認した。同種の装飾パターン(固定幅+`translateX`)が他ページに残って
  いないかもスクリプトで横断確認済み(該当なし)。

### 29-2. 再確認した内容(コード変更なし)

- 全12ページの`<div>`開閉数一致、全15箇所のインラインJS構文
  (`node --check`)、全ページ横断のリンク切れ・アンカー不整合、i18n対応
  8ページの辞書キー過不足を再スクリプト確認。いずれも異常なし。
- バックエンド全ファイルの構文チェック(`py_compile`)、および27章で
  修正した`.env`読み込み処理・タイムゾーン比較処理・本番プレースホルダー
  秘密鍵ガードが、いずれもコード上に正しく残っていることを再確認。
- 同梱の検証ツール`tools/verify_app_html.py`を実際に実行し、`03-app.html`
  のCDNモック経由の動作確認が今回のパッケージに対しても引き続き機能する
  ことを確認(結果: OK、JSエラーなし)。あわせて`03-app.html`について
  モバイル幅(390px)での横スクロール有無も同様に検査し、問題なし。
- **今回の検証環境も、これまでの回と同様にネットワーク制限があり
  (`pip install`不可)、`flask-sqlalchemy`・`pytest`・`stripe`・`sendgrid`
  は未インストールのままだった。そのため、27章で「19件全て合格」を確認
  済みのバックエンドのテストスイートを、今回はコード上の静的レビューでの
  再確認に留めており、実際に再実行はできていない。** 実行結果自体は
  27章の記録を正としてよいが、次にネットワーク制限のない環境を確保
  できた際は、`pytest tests/`の再実行で最終的な裏取りをすることを推奨する。

### 29-3. 公開前に必須の対応(変わらず・再掲)

これまでの回で繰り返し記載してきた下記の対応は、依頼者が実際に企業として
運用する場合にのみ必要であり(20章の通り現状はポートフォリオ/デモ用途と
のことなので、その場合は対応不要):

- `robots.txt` / `sitemap.xml` / 各ページのOGP・canonicalタグにある
  仮ドメイン`https://highlight-studio.example.com`を本番ドメインに置換。
- お問い合わせ用メールアドレス`support@highlight-studio.example.com`
  (複数ページに埋め込み済み)を実際に受信できるアドレスに置換。
- 09ページ・11ページの事業者情報(現状は架空データ「株式会社ハイライト
  スタジオ / 佐藤 直哉」)を、実在の事業者として運用するなら実データに
  差し替え、デモ用架空データである旨の注記を削除。
- バックエンド(決済・メール送信・DB永続化)は現状すべてモックの叩き台
  段階(4-2参照)。実際にサービスとして課金・ユーザー管理を行うには、
  ここからの実装が別途必要。

### 29-4. まとめ

29-1のバグ修正により、**これまでの回で確認していなかった「実際の
ブラウザでの横スクロール有無」という観点で、フロントエンド全12ページに
問題がないことを初めて機械的に確認できた状態**になった。現状把握している
範囲で、静的プロトタイプ(ポートフォリオ/デモ用途)として公開することに
支障のある不具合は残っていないと判断する。次に着手するとすれば、
24-5・28-4で繰り返し引き継いでいる`03-app.html`の多言語対応と、
実ネットワーク環境でのステップ2〜4(検出実行・字幕編集・書き出し)の
E2E動作確認が候補として残る。

## 30. 実インタラクション試験によるデバッグ(2026-09-08・12回目)

29章までは「壊れていないか」を静的チェック中心に確認していたが、今回は
「実際にボタンを押して意図通りに動くか」をPlaywrightで一通り操作して
確認した。

### 30-1. 試験した内容

- `02-auth.html`: 新規登録フォームの未入力エラー表示(赤枠)、ログイン導線。
- `10-contact.html`: 未入力時のエラー表示、正常値での送信→送信完了画面表示。
- `06-pricing.html`: FAQアコーディオンの開閉。
- `04-reset-password.html`: メールアドレス入力→送信の一連の操作。
- `05-mypage.html`: プロフィール編集(表示名変更・メールアドレス変更・
  新パスワード不一致エラー)、お支払い方法の登録(カード番号・有効期限・
  セキュリティコードのバリデーションと登録後表示の切り替え)、
  アカウント削除(同意チェック→確認ボタン活性化→削除実行→
  `localStorage`のクリア→トップページへリダイレクト→削除完了トースト表示)
  の一連の流れ。
- `404.html`・`09-tokushoho.html`・`11-about.html`の表示、`01-landing.html`
  の言語切り替えボタン。

### 30-2. 結果

**上記のいずれについても、コンソールエラー・JS例外は発生せず、想定通りの
挙動(バリデーション表示、モーダルの開閉、データの保存・削除、画面遷移)を
確認できた。今回のデバッグでは新たな不具合は見つからなかった。**

24章で修正した「アカウント削除でReferenceErrorが発生し退会フローが
機能しない」バグについても、削除確認→`localStorage`のクリア→トップページ
での削除完了トースト表示までの一連の流れを実際に操作して問題ないことを
再確認した。

### 30-3. 対象範囲

- コード変更なし(確認のみ)。29章で修正した`01-landing.html`の
  `overflow:hidden`はそのまま有効。

## 31. Vercel公開後のバグ報告への対応 + ランディングページのファイル名変更対応(2026-09-08・13回目)

公開後にユーザーからスクリーンショット付きでバグ報告があり、以下を修正した。

### 修正したバグ
- **ロゴ(左上のタイトル)が未ログイン/ログイン/一部ページでクリック不可だった**: `highlight-studio-01-landing.html`(現`index.html`)・`02-auth.html`・`04-reset-password.html`の3ページで、ヘッダーのロゴが`<div>`のままリンクになっていなかった。`<a>`タグ化した上で、全12ページに「未ログインならトップ、ログイン済みならアプリ本体(`03-app.html`)へ遷移」というログイン状態に応じたリンク先切り替えスクリプトを追加し、サイト全体で統一した。
- **トップページ右上に「出」とだけ表示される謎の表示崩れ**: ヒーローのスマホ風モックアップに浮かせている「盛り上がり自動検出」バッジ(`.chip-detect`)が`z-index`未指定のため、後から描画される`.phone-frame`の背景に隠れ、はみ出た最後の1文字だけが見えていた。`.float-chip`に`z-index:5`を追加し、常に手前に表示されるよう修正。あわせてオフセットを`chip-detect: right:-14px→-38px`、`chip-caption: left:-30px→-54px`に広げ、顔カメラの丸モック(`.face-cam`)との重なりも軽減した。`white-space:nowrap`も追加。

### ファイル名変更への追随
ユーザーがGitHub上でランディングページのファイル名を`highlight-studio-01-landing.html`→`index.html`に変更したため、他11ページ+ランディング自身に残っていた同名参照(ロゴ、フッター各リンク、「トップに戻る」系リンク、料金ページの`#steps`/`#features`アンカー、ログイン状態判定JSの遷移先、og:url/canonicalメタタグ)を全て`index.html`に一括置換。これで`highlight-studio-01-landing.html`という文字列は本パッケージ内に残っていない。

### 引き継ぎ事項
- 現状、ランディングページのファイル名は`index.html`が正式。今後この一式を再度Vercel等にデプロイする際は、ルート直下に`index.html`として置く想定(サブディレクトリ`frontend/`ごと公開する場合は`frontend/index.html`がルートの`index.html`として解決される設定になっているか要確認)。
- z-index修正は視覚的な応急処置。オフセット値(`-38px`/`-54px`)は目視確認をしていないため、実際の見え方次第で微調整が必要な場合がある。

## 32. お問い合わせ手段のGoogleフォーム化 + 特商法表記の整理 + 広告収益モデルへの方針転換(2026-09-08・14回目)

運営者(個人)から「問い合わせ先のメールアドレスが実在しない」「特商法表記に架空の会社名・代表者名が入っている」という指摘があり、対応した。途中で「有料プランをやめて広告収益の無料サービスにしたい」という方針転換の指示が入ったため、関連するUI・文言もあわせて修正した。

### 32-1. お問い合わせ手段をGoogleフォームに変更
- 実在しない`support@highlight-studio.example.com`(mailto)を、運営者が別途作成したGoogleフォームへの導線に差し替えた。フォームURL: `https://docs.google.com/forms/d/e/1FAIpQLSftwsSvulvMiHea5cXrFNdbWbUFYE5CiM0IdlBAS4wQLNmzXw/viewform?usp=publish-editor`
- `highlight-studio-10-contact.html`: 自前実装だった入力フォーム(名前・メール・種別・内容 → `backend/app/routes/contact.py`へPOST)を撤去し、「お問い合わせフォームを開く」ボタン1つでGoogleフォームを新規タブで開く構成に変更。フォーム送信まわりのJS(`validateEmail`、送信ハンドラ、`HSApi.submitContact`呼び出し、成功画面切り替え)と`api-client.js`の読み込みを削除。
- 回答期限の文言を「5日以内に確認、内容によっては返信しない場合がある」旨に変更(以前の「1〜3営業日以内に返信」という確約表現をやめた)。
- `highlight-studio-09-tokushoho.html`・`highlight-studio-08-privacy.html`の連絡先欄も同じGoogleフォームリンクに差し替え済み(その後32-3で09-tokushoho.htmlは削除方針)。

### 32-2. 特商法表記の氏名非公開対応(個人事業主・広告モデル転換前の暫定対応)
- 運営者は個人で氏名を公開したくない意向のため、消費者庁ガイドライン(2022年6月〜)に基づき、屋号(`Highlight Studio`)表示 + 氏名・所在地は「請求があれば遅滞なく開示する」形式に変更。開示請求の窓口は上記Googleフォーム。
- `highlight-studio-09-tokushoho.html`・`highlight-studio-11-about.html`(運営事業者情報テーブル)の両方で、架空の「株式会社ハイライトスタジオ」「代表取締役 佐藤 直哉」表記を修正。
- ※この対応は32-3の広告モデルへの転換により、最終的には特商法ページ自体の削除という形に落ち着いている。過渡的な対応として記録を残す。

### 32-3. 有料プラン廃止・広告収益モデルへの転換(方針: B)
運営者の判断で「月額課金はやめて、無料+広告収益のサービスにする」ことになった。特定商取引法は「消費者から対価を得る通信販売」が対象のため、**無料化すれば特商法表記そのものが不要**という整理のもとで対応した。

**削除・非表示にしたもの**
- `highlight-studio-09-tokushoho.html`への導線を全ページ(index/app/mypage/terms/privacy/contact/about/sitemap.xml)から削除。**ファイル自体の削除はまだ完了していない**(次回引き継ぎ参照)。
- `highlight-studio-11-about.html`の「運営事業者情報」テーブル(屋号・運営者・所在地・事業内容・連絡先)をセクションごと削除。セクション番号を振り直し済み(旧4→3)。
- `index.html` / `highlight-studio-03-app.html`のヘッダーから「早期アクセス」プランピルを削除。
- `highlight-studio-05-mypage.html`のヘッダープランピルをリンクなしのバッジ表示に変更。

**「使わない状態で温存」にしたもの(削除はしていない)**
- `highlight-studio-06-pricing.html`・`backend/app/routes/billing.py`本体は未着手。将来の有料プラン復活時にそのまま使える想定。
- `highlight-studio-03-app.html`: `currentPlan`の判定を`localStorage.getItem('hs_plan')`ベースから`'pro'`固定に変更(該当行はコメントアウトで残し、1行のコメントアウト解除で元に戻せる)。これにより無料プラン向けの透かし表示・画質/字幕スタイル/音声認識モデルのロック・アップグレード誘導が全て無効化されている。
- `highlight-studio-05-mypage.html`: `PLAN_LIMITS`に無制限の`adfree`プランを追加し、`currentPlanKey`をこれに固定(同じくコメントアウトで元の判定ロジックを保持)。`admin`プラン用に既にあった「アップグレード・お支払いボタンを隠す」分岐に`adfree`も追加する形で流用。決済モーダル(`billingModal`)自体のマークアップ・JSは未削除(ボタンが出ないため到達不能な状態)。
- `backend/app/routes/billing.py`のBlueprint登録(`app/__init__.py`側)は未確認・未変更。フロントから叩かれなくなっただけで、エンドポイント自体は生きている可能性が高い。

**文言・規約まわり**
- `highlight-studio-07-terms.html`: 「料金プラン・お支払い」の章を「ご利用料金・広告について」に書き換え(無料・広告表示ありの旨、特商法への参照を削除)。
- `highlight-studio-08-privacy.html`: 取得情報からお支払い情報の項目を削除し、Cookie利用の章に広告配信事業者によるCookie利用の可能性を追記(広告ネットワーク未定のため、事業者名は「導入次第追ってご案内」という書き方にしてある)。
- `highlight-studio-10-contact.html`: 料金プランFAQへの案内カードを削除。

**広告枠のプレースホルダー**
- 広告ネットワークが未定のため、実際の広告タグは未実装。`id="ad-slot-1"`(`index.html`、compareセクション直前)と`id="ad-slot-export"`(`highlight-studio-03-app.html`、書き出し完了パネル内)に、破線枠のプレースホルダーdivを設置済み。広告ネットワーク導入時はこの2箇所に配信タグを差し込むだけでよい想定。

### 32-4. 未対応・次回引き継ぎ事項
- **`highlight-studio-09-tokushoho.html`のファイル削除が未完了**。参照は全ページから外したが、ファイル自体はパッケージ内に残っている。次回、忘れずに削除すること。
- 広告ネットワーク未確定。決まり次第、`ad-slot-1`・`ad-slot-export`への実装と、`highlight-studio-08-privacy.html`のCookie章(「導入次第追ってご案内」の部分)を実際の事業者名・オプトアウト方法の記載に更新する必要がある。
- `backend/app/routes/billing.py`・`highlight-studio-06-pricing.html`は意図的に未着手。Blueprint登録を外すか等の判断は次回検討。
- `highlight-studio-05-mypage.html`の決済モーダル(カード登録デモ)は表示経路がなくなっただけで中身は残存。完全に無料サービスとして割り切るなら、いずれこのモーダルごと削除してよい。
- 個人事業主として運営する場合の各種届出(開業届など)や、広告収益が発生した際の確定申告まわりは本メモの対象外。税理士等に別途確認が必要。

## 33. 本番公開に向けた最終仕上げ(2026-09-08・15回目)

依頼者から「サービスは`https://highlight-studio.vercel.app/`で公開済み。やれることからやって」との指示があり、対応した。

### 見つけて直したバグ
- **`06-pricing.html`のフッターに、削除したはずの特商法ページへのリンクが2箇所残っていた**: 32-3で「index/app/mypage/terms/privacy/contact/about/sitemap.xmlから導線を削除」と記載されていたが、この対象リストに`06-pricing.html`が含まれておらず、フッターの通常表示・省略表示の2箇所にリンクが残存していた。他ページと同じ形に削除。
- **`sitemap.xml`が旧ファイル名`highlight-studio-01-landing.html`を指したままだった**: 31章でランディングページを`index.html`にリネームした際の一括置換対象にsitemap.xmlが含まれておらず、存在しないファイルへのリンクが残っていた。`index.html`に修正。
- **`07-terms.html`に旧お問い合わせ用メールアドレス(`support@highlight-studio.example.com`のmailtoリンク)が残っていた**: 32-1で`10-contact.html`・`09-tokushoho.html`・`08-privacy.html`はGoogleフォーム導線に置換されたが、`07-terms.html`だけ対象から漏れていた。他ページと同じGoogleフォームリンクに統一。

### 実施した対応
- `09-tokushoho.html`のファイル削除(32-4で積み残しになっていたタスク。全ページからの参照が既に無いことを確認した上で削除)。
- 仮ドメイン`https://highlight-studio.example.com`を本番ドメイン`https://highlight-studio.vercel.app`に一括置換(`og:url`/`og:image`/`twitter:image`/`canonical`/`sitemap.xml`の全`<loc>`/`robots.txt`のSitemap行、計9ファイル)。

### 確認方法
- 置換後、`example.com`の残存を全文検索 → 残っているのは`you@example.com`(メール入力欄のプレースホルダー文言)と`api-client.js`内のサンプルコメントのみで、いずれも無害であることを確認。
- 編集した`06-pricing.html`・`07-terms.html`のインラインJSを`node --check`で構文確認、異常なし。

### 未対応(変わらず引き継ぎ)
- ネットワーク制限により、バックエンドの`pip install -r requirements.txt && pytest`は今回も実行できていない(27章の記録が最後の実測)。
- 広告ネットワーク未確定、`backend/app/routes/billing.py`・`06-pricing.html`・`05-mypage.html`の決済モーダルは意図的に未着手のまま(将来の有料プラン復活用に温存)。

## 34. 会員登録を全廃止 + 広告視聴ゲートによる収益化への転換(2026-09-08・16回目)

依頼者から「会員登録に関するものは全て削除。アップロード・編集・ダウンロードだけのサイトにし、収益化として広告視聴を都度挟む」という明確な方針指示があり、対応した。32-33章で「有料プラン廃止・広告収益モデルへ」という方針転換はすでに行われていたが、認証(ログイン/新規登録/マイページ)自体は温存されていた。今回はそれも含めて完全に撤去した。

### 34-1. 削除したページ・ファイル
- `highlight-studio-02-auth.html`(ログイン・新規登録)
- `highlight-studio-04-reset-password.html`(パスワード再設定)
- `highlight-studio-05-mypage.html`(マイページ)
- `highlight-studio-06-pricing.html`(料金プラン)
- `frontend/api-client.js`(認証・決済・パスワード再設定用のAPIクライアント。参照していたのがすべて上記の削除ページのみだったため、読み込んでいるページが存在しなくなり丸ごと削除)
- **`backend/`ディレクトリ全体**(Flask製バックエンド叩き台一式)。認証(`auth.py`)・決済(`billing.py`)はいずれも上記の削除ページ専用の機能で、フロント側の接続経路(`api-client.js`)ごと消えたため接続先を失った。お問い合わせ(`contact.py`)も32-1でGoogleフォーム化済みで既に未使用だった。会員登録を伴わない静的サイト運用に一本化したため、バックエンド自体を撤去した。将来サーバーサイド機能(実際の動画処理オフロード等)が必要になった場合は、過去のHANDOFF(6章・6-1章など)にFlask叩き台の設計方針が残っているので参照可能。

### 34-2. `highlight-studio-03-app.html`(アプリ本体)の変更
- ヘッダーのアカウントメニュー(アバターボタン・マイページ導線・ログアウト)を削除。
- 起動時の`hs_loggedIn`チェック(未ログインなら`02-auth.html`へ強制リダイレクト)を削除。ページを直接開けば誰でもすぐ使える。
- プラン制御を全廃:`PLAN_LIMITS`/`currentPlan`/`monthlyLimit`、月間書き出し本数のカウント・上限バナー、無料プランの透かし焼き込み、画質・字幕スタイル・音声認識モデルのロックUIをすべて削除。全機能が誰でも無条件で使える状態にした。
- 新規実装: **広告視聴ゲート**(`showAdGate()`)。「書き出し開始」ボタンと、ダウンロード操作(クリップ個別・まとめての両方)のたびに、広告プレースホルダー付きのオーバーレイを表示し、5秒のカウントダウン後に「続ける」ボタンが押せるようになる仕組みを追加。広告ネットワークが未確定なため、現状はモック(プレースホルダー枠+カウントダウン)。実際の広告タグを導入する際は、`#adGateOverlay`内のプレースホルダー`div`に配信タグを差し込み、`showAdGate()`のコールバック実行タイミング(広告視聴完了とみなすタイミング)を実際の広告SDKのコールバックに置き換えること。

### 34-3. `index.html`(ランディング)の変更
- ヘッダーの「ログイン」ボタンを削除。「無料で始める」はアプリ本体へ直接遷移するだけのボタンに変更(以前はログイン状態でボタンの文言・遷移先を出し分けていたが、その分岐ごと削除)。
- フッターの「アカウント」列(ログイン/無料で始める/マイページの3リンク)を削除。
- 最終CTAの文言を「アカウントを作成して」→「登録不要、今すぐ」に変更。「クレジットカードの登録は不要です」→「会員登録・クレジットカードの登録は不要です」に変更。

### 34-4. 規約・プライバシーポリシーの変更
- `highlight-studio-07-terms.html`: 「アカウント登録」章(旧3章)を丸ごと削除し、章番号を詰めた。「適用」章・「サービス内容」章に「会員登録は不要」である旨を明記。賠償責任上限額の条項から「無料プランをご利用の場合は」という前提を削除し、単純に0円と明記(プラン概念自体が無いため)。
- `highlight-studio-08-privacy.html`: 取得する情報からメールアドレス・氏名・Googleアカウント連携情報を削除し、「会員登録を必要としない」旨を明記。Cookieの利用目的から「ログイン状態の維持」を削除(広告配信・アクセス状況把握のみに)。データ保存期間・ユーザーの権利の章から「アカウント」「マイページ」への言及を削除。
- `highlight-studio-10-contact.html`: メタタグ・リード文にあった「料金プランに関するご質問」という文言を削除(プラン自体が存在しなくなったため)。

### 34-5. その他の整頓
- `404.html`・`highlight-studio-07-terms.html`・`highlight-studio-08-privacy.html`・`highlight-studio-10-contact.html`・`highlight-studio-11-about.html`に残っていた「ログイン状態に応じてロゴのリンク先を切り替える」死んだコード(ログイン機能自体が無くなり、常にトップページ固定になるだけの無意味な分岐)を5ファイルから削除。
- `i18n-common.js`から未使用になった共通キー(`common.earlyAccess`・`common.login`・`common.footerPricing`・`common.footerColAccount`・`common.footerMypage`)を削除。
- `tools/verify_app_html.py`: 存在しなくなった`highlight-studio-01-landing.html`(実際は`index.html`)を経由したログイン状態セットアップ処理を削除し、`highlight-studio-03-app.html`を直接開いて検証する形に更新。

### 34-6. 確認方法
- 全7ページ(`index`, `03-app`, `07-terms`, `08-privacy`, `10-contact`, `11-about`, `404`)について、`02-auth`/`04-reset-password`/`05-mypage`/`06-pricing`への参照が残っていないことをスクリプトで全文検索して確認(0件)。
- 全ページの内部リンク(`href`/`src`)が実在するファイルを指しているか機械チェック(異常なし)。
- 全ページのHTML要素IDの重複チェック(異常なし)。
- 全ページの全インラインJS(`<script type="module">`含む)を`node --check`相当の構文チェックで確認(異常なし)。
- 各ページで使われている`data-i18n`系キーが、`i18n-common.js`またはページ固有辞書のいずれかで定義されていることを機械チェック(不足なし)。
- **未実施**: 実ブラウザでのE2E動作確認(広告ゲートの表示・カウントダウン・続行ボタンの動作、書き出し→ダウンロードの一連の流れ)。ネットワーク制限のためPlaywrightでの実行ができておらず、静的チェックの範囲にとどまっている。次回、実行可能な環境で`tools/verify_app_html.py`の拡張、または手動での動作確認を推奨。

### 34-7. 未対応・次回引き継ぎ事項
- 広告ネットワーク未確定。決まり次第、`highlight-studio-03-app.html`の`#adGateOverlay`内プレースホルダーと`index.html`の`#ad-slot-1`・`highlight-studio-03-app.html`の`#ad-slot-export`に実際の広告タグを実装し、`showAdGate()`のコールバックタイミングを広告SDKの視聴完了イベントに合わせて調整すること。
- 広告視聴ゲートの待機時間(現状5秒固定)・挿入タイミング(現状:書き出し開始時、ダウンロード時)は暫定値。実際の広告ネットワーク・収益モデルが決まった段階で、ユーザー体験と収益のバランスを見て調整することを推奨。
- バックエンドを撤去したため、`README.md`等でバックエンドに言及している箇所があれば実態と合わなくなっている可能性がある(本HANDOFF.md自体の6章・6-1章・27章などは過去の記録として残しているが、現状のパッケージにはバックエンドは含まれていない点に注意)。

## 35. 不具合修正: 累計アクセスカウンター表示 + 書き出し時のffmpeg同時実行エラー(2026-09-08・18回目)

依頼者から2件の不具合報告を受けてデバッグした。

### 35-1. 経緯
- 依頼者からの最初の報告は「カウンター見つからないし、無料で始めるの先のリンクがない」という短いもので、原因不明のまま2つ目の症状(リンクがない)は再現できなかった。
- 手元で`index.html`をPlaywrightで実ブラウザ起動して検証した結果、「無料で始める」の3ボタン(ヘッダー・ヒーロー・下部CTA)はいずれもコード上・実機とも正常に`highlight-studio-03-app.html`へ遷移することを確認。ここでは原因を特定できず、依頼者に「押すと実際どうなるか」を選択式で確認してもらったところ「404など別のURLに飛ぶ」という回答だったが、その後依頼者から実際のスクリーンショットが提供され、真因は404ではなく**書き出しステップでのffmpeg失敗**であったと判明した(依頼者の「リンクがない」という表現は、書き出しが失敗してダウンロードリンクが出てこない状態を指していたと考えられる)。

### 35-2. `index.html`: 累計アクセスカウンターが常に非表示になる不具合
- **原因**: フッターのレトロ風カウンターは外部の無料カウンターAPI `countapi.xyz` にアクセス記録を都度取得しに行く実装だったが、`countapi.xyz`は既にサービス終了しており(名前解決自体が失敗する)、取得が常に失敗していた。実装は「取得失敗時は見た目が壊れないよう要素ごと非表示にする」という設計だったため、これが裏目に出て**恒久的に非表示**になっていた。
- **修正**: 外部APIへの依存を廃止し、`localStorage`(キー: `hs_retro_counter_local_visits`)だけで完結する「このブラウザでの累計アクセス」カウンターに変更。ページ読み込みのたびに+1してその場で表示する。ラベル文言(`landing.retroCounterLabel`)も実態(サイト全体ではなくこのブラウザでの回数)に合わせて修正。
- **追加対応**: 依頼者からの要望で、フッターに加えてページ最上部(`<header>`より前)にも同じカウンターを追加表示。`.retro-counter`クラスを持つ要素すべてに同じ値を反映するよう、カウンター描画ロジックを`document.querySelectorAll('.retro-counter')`ベースに変更(IDベースの単一要素前提だった実装を複数箇所対応に一般化)。
- **確認方法**: Playwrightで実ブラウザ起動しヘッドレス検証。修正前は`#retroCounter`が`hidden=true`のまま(コンソールに`Failed to fetch`警告)、修正後はページ最上部・フッターの両方に`000001`→リロードで`000002`と表示され、カウントアップすることを確認。

### 35-3. `highlight-studio-03-app.html`: 書き出し失敗「ffmpeg.wasm can only run one command at a time」
- **原因**: ffmpeg.wasmは1つのインスタンスに対して同時に2つ以上の`run()`を実行しようとすると即座にこのエラーで失敗する。既存の`runFfmpegSafe`ヘルパーは「exit(0)系の不安定エラーが出たら読み込み直して1回だけ自動再試行する」保険は備えていたが、**呼び出しそのものが重なることを防ぐ仕組み(直列化)がなく**、「もう一度試す」の連打や処理タイミングの重なりでffmpegの呼び出しが同時に走るとこのエラーで即失敗していた。
- **修正**:
  1. `runFfmpegSafe`: すべてのffmpeg呼び出しを共有の実行キュー(Promiseチェーン)に通すように変更し、呼び出し元がいくつあっても実行は必ず1つずつ順番になるようにした。あわせて、自動再試行の対象パターンに`can only run one command at a time`も追加。
  2. `startBurn`: `burnInProgress`フラグを追加し、「書き出し開始」「もう一度試す」を連打しても`runBurnAll`が二重に走らないようにした(あわせて連打防止のため書き出し中は「もう一度試す」ボタンも無効化するようにした)。
- **確認方法**: 上記ロジック相当を単体のNodeスクリプトに抜き出し、「実行中に別のrun()が呼ばれると`can only run one command at a time`を投げる」フェイクffmpegに対して5件を意図的に同時発火させるテストを実施。修正前ロジックでは1件目以外がすべて同エラーで失敗することを再現し、修正後ロジックでは5件全てが正常に順番実行されることを確認した。`highlight-studio-03-app.html`本体は`node --check`で構文確認済み(異常なし)。
- **未確認・引き継ぎ事項**: 上記はロジックを抜き出した単体テストでの確認であり、実際の`highlight-studio-03-app.html`を実ブラウザ+実ffmpeg.wasmで書き出しまで通しての動作確認はできていない(音声認識モデル・ffmpeg.wasm本体ともCDN取得が必要で、検証環境のネットワーク制限により未実施)。依頼者に実機での再現確認を依頼中。もし修正後も同エラーが再発する場合は、その時の操作手順(どのボタンをどの順番で押したか、特に「もう一度試す」やステップ間の行き来があったか)を聞き取ること。

## 36. 書き出し失敗「Program terminated with exit(1)」の調査 + エラー診断情報の強化(2026-09-08・19回目)

依頼者から、書き出しステップで「書き出しに失敗しました / Program terminated with
exit(1) — もう一度試してください」というエラーバナーのスクリーンショットが
届いた。35章で対応した「can only run one command at a time」(exit(0)系の
不安定挙動)とは別物で、**exit(1)はffmpegコマンド自体が実際に失敗している
ことを示すコード**であり、既存の`runFfmpegSafe`は意図的にこのケースを
自動再試行の対象にしていない(中身のない`exit(0)`と違い、同じコマンドを
再試行しても同じ理由で失敗するだけのため)。

### 36-1. 根本原因は未特定
今回渡されたスクリーンショットの「処理ログを表示」は未展開で、かつ展開しても
表示されるのはアプリ側の進行ログ(「クリップ1: 字幕を焼き込み中...」等)のみで、
**ffmpeg自身がstderrに出す実際の失敗理由(未対応オプション/フィルタ構文エラー/
存在しないストリームの指定など)は画面上のどこにも表示されておらず、
ブラウザの開発者ツールのコンソールを開かないと見えない**状態だった。
コードを読んで洗い出した「exit(1)になりうる」候補(字幕テキストにフィルタ
構文を壊す文字が含まれる、クロップ後のクリップに音声トラックが無く
`-c:a copy`が失敗する、等)はいくつかあるが、実際にどれが今回の原因かは
この情報だけでは確定できなかった。依頼者に実機で再現してもらうにも、
今のままでは「exit(1)」としか報告できず、原因調査のたびに開発者ツールの
案内から始める必要があり非効率と判断し、今回は原因特定より先に
**次に同じことが起きたときに一目で原因が分かる仕組み**を用意した。

### 36-2. 実施した対応: ffmpeg内部ログをエラーバナーに表示
- `ensureFfmpegLoaded`でffmpegインスタンス作成直後に`ffmpeg.setLogger(...)`を
  設定し、ffmpeg.wasm本体が出す内部ログ(stdout/stderr相当)を直近40行分だけ
  `ffmpegLogBuffer`に保持するようにした。
- `runFfmpegSafe`内で、exit(0)系(自動再試行対象)ではない本物の失敗を
  投げ直す直前に、保持しておいたffmpeg内部ログの末尾8行をエラーメッセージに
  追記するように変更。これにより`startDetect`・`startBurn`双方の
  エラーバナー(`waveErrorDesc`・`burnErrorDesc`)に、ffmpegの実際の失敗理由が
  そのまま表示されるようになる。
- エラーバナーの`.eb-desc`に`white-space:pre-wrap`を追加し、複数行のログが
  改行を保ったまま読める形で表示されるようにした(元は1行のtextContentしか
  想定していないスタイルだった)。
- `highlight-studio-03-app.html`本体のインラインスクリプトを抽出して
  `node --check`で構文確認済み(異常なし)。

### 36-3. 未対応・次回引き継ぎ事項
- **今回の「exit(1)」自体の根本原因はまだ直っていない**。36-2の対応は
  あくまで「次に依頼者が再現したときに、今度こそ本当の原因がエラーバナーに
  出る」ための診断強化であり、修正そのものではない。依頼者に再度お試し
  いただき、今度は展開後のエラーバナーの全文(ffmpegログ末尾を含む)を
  送ってもらう必要がある。
- 洗い出した「exit(1)になりうる」候補のうち特に有力なのは、クロップ後の
  クリップ(`cropped_N.mp4`、STEP2で`-c:a aac`を付けて生成)に音声トラックが
  存在しないケース(元動画に音声が無い、または極端に短い区間で音声ストリームが
  生成されない等)で、書き出し時(STEP4)の`-c:a copy`がコピー元の音声ストリームを
  見つけられず失敗する可能性。次回、実際のログが取れたらまずここを確認すること。
- 字幕テキストの`escapeForDrawtext`は`\`・`:`・`%`・`,`・`'`をエスケープしているが、
  改行文字や`[`・`]`・`;`などフィルタグラフ上で意味を持つ他の文字は未対応のまま。
  文字起こし結果に稀にこうした文字が混ざるとdrawtextのフィルタ構文が壊れて
  exit(1)になる可能性も否定できない。ログが取れ次第、あわせて確認すること。

## 37. 書き出し失敗の続報「ffmpeg.FS('readFile', 'final_0.mp4') error」の原因特定と修正(2026-09-08・20回目)

36章で入れた診断強化(ffmpeg内部ログのエラーバナー表示)を経て、依頼者から
続報のスクリーンショットが届いた。今回のエラーは`Program terminated with
exit(1)`ではなく、`ffmpeg.FS('readFile', 'final_0.mp4') error. Check if the
path exists`だった。

### 37-1. 原因
このエラーは`runFfmpegSafe`の中(=ffmpegコマンド実行そのもの)ではなく、
`runBurnAll`が焼き込み処理の**直後**に呼んでいる`ffmpeg.FS('readFile', outName)`
(書き出したファイルを読み出す処理)で発生していた。つまり`ffmpeg.run()`自体は
**例外を投げずに正常終了**していたにもかかわらず、出力ファイル(`final_0.mp4`)が
仮想ファイルシステム上に存在しなかった。

これは35〜36章で扱ってきた「ffmpeg.wasm(特にcore-st・1コア版)を同じ
インスタンスで繰り返し`run()`すると内部状態がおかしくなる」という**同じ
既知の不具合の、また別の現れ方**だと判断した。これまでは
「`Program terminated with exit(0)`」という例外を投げるパターンと
「`can only run one command at a time`」という例外を投げるパターンの2つに
対して「読み込み直して1回だけ再試行」を行っていたが、**今回は例外すら
投げずに黙って出力ファイルだけが欠落する**という3つ目のパターンだったため、
既存の再試行ロジックの対象に入っておらず、そのままエラーバナー行きになっていた。

### 37-2. 修正内容
- `runFfmpegSafe(preset, args, outputName)`に第3引数`outputName`を追加。
  渡された場合、`ffmpeg.run()`が例外なく終わったあとに
  `ffmpeg.FS('readFile', outputName)`で出力ファイルの存在も検証するように変更。
  読み出しに失敗した場合も、既存のexit(0)系と同じ「ffmpegインスタンスを
  読み込み直して同じコマンドを1回だけ自動再試行」の対象として扱う。
- 呼び出し元3箇所(`extractAudioSnippet16k`の音声抽出、STEP2の縦動画切り抜き、
  STEP4の字幕焼き込み書き出し)すべてに`outputName`を渡すように変更。
  いずれも「run()の直後に同じファイル名をreadFileしている」箇所だったため、
  今回発覚した書き出し(STEP4)だけでなく、音声解析・切り抜き段階で同種の
  不具合が起きた場合にも同じ再試行で救えるようになった。
- 再試行後もなお失敗する場合は、36章で追加したffmpeg内部ログの末尾を
  エラーメッセージに付け足したうえで例外を投げるようにし、それでも
  ダメだったケースの原因調査もできるようにした。
- ロジック相当を切り出したNodeスクリプトで、「1回目は例外を投げずに出力
  ファイルを書かない、2回目は正常に書く」というフェイクffmpegに対して
  実際に1回だけ自動再試行され、最終的に`readFile`が成功することを確認済み。
- `highlight-studio-03-app.html`本体のインラインスクリプトは
  `node --check`で構文確認済み(異常なし)。

### 37-3. 未対応・次回引き継ぎ事項
- 上記は依頼者から提供されたスクリーンショットの症状(FSレベルでの
  ファイル欠落)と、既知の不安定バグの挙動パターンから導いた対応であり、
  実際の`highlight-studio-03-app.html`を実ブラウザ+実ffmpeg.wasmで
  書き出しまで通しての動作確認はできていない(検証環境のネットワーク制限で
  CDN取得が必要なffmpeg.wasm本体・音声認識モデルが使えないため)。
  依頼者に実機での再現確認を依頼中。
- 万が一、再試行後(2回目の実行)でも出力ファイルが生成されない場合は、
  今度こそffmpeg内部ログの末尾がエラーバナーに表示されるはずなので、
  その全文を送ってもらうこと。

## 38. 真の原因判明「cropped_0.mp4: No such file or directory」+ 前回入れた再試行機構自体の不具合を修正(2026-09-08・21回目)

36〜37章の診断強化のおかげで、依頼者からのスクリーンショットにffmpeg内部ログの
末尾が表示されるようになり、初めて具体的な失敗理由が判明した。

```
cropped_0.mp4: No such file or directory
```

### 38-1. 判明した根本原因
書き出し(STEP4)のffmpegコマンドは`-i cropped_0.mp4`のようにSTEP2で
切り抜き済みのクリップを入力に取る。ところが**37章で追加した「不安定挙動を
検知したらffmpegインスタンスを読み込み直して1回だけ再試行する」処理は、
`ensureFfmpegLoaded`経由で`input.mp4`だけを新しいインスタンスに書き直し、
STEP2で作った中間ファイル(`cropped_0.mp4`など)は書き直していなかった**。
ffmpegインスタンスを読み込み直すと仮想ファイルシステムは完全に初期化される
ため、読み込み直した直後の再試行では`cropped_0.mp4`がそもそも存在せず、
「入力ファイルが見つからない」という新たな失敗を起こしていた。

つまり37章の対応は、本来救おうとしていた不具合(出力ファイルが生成されない)
に対して、書き出し(STEP4)に限っては**再試行のたびに別の失敗を確実に
引き起こしてしまう**という抜け穴があった。

### 38-2. 修正内容
- `runFfmpegSafe(preset, args, outputName, inputFiles)`に第4引数
  `inputFiles`(`{name, data}`の配列)を追加。渡された場合、`ffmpeg.run()`を
  試みる直前(初回・読み込み直し後の再試行の両方)に、必ず該当ファイルを
  仮想ファイルシステムへ書き直してから実行するようにした。これにより、
  途中でffmpegインスタンスが作り直されても、次のrun()の直前に必要な
  中間ファイルが復元される。
- STEP2(切り抜き)側で、`cropped_N.mp4`を`ffmpeg.FS('readFile', ...)`で
  読み出した際に、そのバイト列(`data.slice()`でWASM側のメモリから独立した
  コピーを取ったもの)を`clip.croppedData`としてJS側(クリップのオブジェクト)
  にも保持するように変更。これまでは再生プレビュー用のBlob URLしか
  保持していなかったため、ffmpegの仮想ファイルシステム上のコピーが唯一の
  実体だった。
- STEP4(書き出し)の`runFfmpegSafe`呼び出しに
  `inputFiles: [{ name: clip.croppedFile, data: clip.croppedData }]`を渡し、
  読み込み直しが起きても`cropped_N.mp4`を復元できるようにした。
- ロジック相当を切り出したNodeスクリプトで、「新規に読み込み直された
  ffmpegインスタンスのFSには`input.mp4`しか無い(=croppedファイルは消えている)」
  状況を再現し、`inputFiles`経由での書き直しによって2回目の実行で
  正しく`cropped_0.mp4`が復元されて書き出しが成功することを確認済み。
- `highlight-studio-03-app.html`本体のインラインスクリプトは
  `node --check`で構文確認済み(異常なし)。

### 38-3. 未対応・次回引き継ぎ事項
- 上記は依頼者提供のログから特定した確実な原因であり、修正ロジックも
  単体テストで動作を確認済みだが、実際の`highlight-studio-03-app.html`を
  実ブラウザ+実ffmpeg.wasmで書き出しまで通しての動作確認はまだできていない
  (検証環境のネットワーク制限のため)。依頼者に実機での再現確認を依頼中。
- 今回の教訓として、「ffmpegインスタンスを読み込み直す」系の再試行ロジックを
  今後さらに拡張する場合(例: STEP2の切り抜き自体が同じ不安定挙動で
  失敗した場合の再試行など)は、その処理が依存する中間ファイルがすべて
  `inputFiles`経由で復元対象に入っているか都度確認すること。

## 39. 38章の修正だけでは不十分だった問題を修正: 復元を「書き出し個別」から「ffmpeg読み込み直し全体」に一本化(2026-09-08・22回目)

依頼者から38章の修正を試した結果として2枚のスクリーンショットが届き、
どちらも**同じ`cropped_0.mp4: No such file or directory`がまだ起きている**
ことが判明した。

### 39-1. 38章の修正で不十分だった理由
38章では「書き出し(STEP4)がffmpegを読み込み直すとき」に限って
`cropped_N.mp4`を復元する対応をした。しかし実際には、**STEP4が始まる
前のSTEP3(音声認識によるキャプション付け)の途中でも同じ「不安定→
読み込み直し」が起こりうる**ことを見落としていた。STEP3の
`extractAudioSnippet16k`(キャプションのチャンクごとに何度も呼ばれる
音声抽出処理)はSTEP4とは別に同じ共有ffmpegインスタンスを使っており、
ここで読み込み直しが起きると、STEP2で作った`cropped_0.mp4`はその時点で
消えてしまう。STEP4自身の1回目の実行が始まった時点ですでにファイルが
無ければ、38章の「STEP4の再試行時だけ復元する」対応は意味をなさない
(1回目からダメで、再試行してもcropped_0.mp4を復元する処理自体は
1回目の失敗を検知したあとにしか走らないため)。

### 39-2. 修正内容: 復元の仕組みを一本化
特定の呼び出し箇所ごとに「このファイルを守る」と個別に対応するのをやめ、
**「ffmpegが読み込み直されるたびに、これまで登録された全ての重要ファイルを
自動で復元する」**という一元的な仕組みに変更した。

- `ensureFfmpegLoaded`内、`input.mp4`を書き直すタイミング(=新しい
  インスタンスが作られた、またはFSがリセットされたタイミングと必ず一致する)
  で、`persistentFiles`という`Map`に登録された全ファイルもまとめて書き直す
  ようにした。
- STEP2(切り抜き)で`cropped_N.mp4`を生成したら、`registerPersistentFile(name, data)`
  で登録するだけでよい。以後、STEP3・STEP4のどこでffmpegが何回読み込み
  直されても、次にファイルが必要になった時点で自動的に復元されている。
- 「もう一度検出しなおす」で`clips`をリセットする箇所に合わせて
  `clearPersistentFiles()`も呼び、動画を差し替えて再検出した際に古い
  クリップのデータが残り続けないようにした。
- 38章で追加した`runFfmpegSafe`の`inputFiles`引数(書き出し個別の復元)は
  無害なので念のため残してあるが、実質的にはこの一元的な仕組みが
  常に先に効くため冗長になっている。
- ロジック相当を切り出したNodeスクリプトで、「STEP2完了後、STEP4とは
  無関係な理由でffmpegが読み込み直される→STEP4の1回目の実行時点で
  `cropped_0.mp4`がすでに正しく復元されている」という、38章のテストでは
  再現できていなかったシナリオを再現し、修正後は1回目から正常に
  書き出しが成功することを確認した。
- `highlight-studio-03-app.html`本体のインラインスクリプトは
  `node --check`で構文確認済み(異常なし)。

### 39-3. 未対応・次回引き継ぎ事項
- 依然として実際の`highlight-studio-03-app.html`を実ブラウザ+実ffmpeg.wasmで
  最初から最後まで通しての動作確認はできていない(検証環境のネットワーク
  制限のため)。依頼者に実機での再現確認を依頼中。
- 今回、2回連続で「対応した箇所は直ったが、別の箇所で同じ問題が
  再発する」という展開になった。これはffmpeg.wasm(特にcore-st)の
  不安定挙動そのものが、共有インスタンスを使うアプリ全体のどこでで
  起きてもおかしくないため。今後また同様の症状(「◯◯.mp4: No such
  file or directory」やそれに類するファイル消失系のエラー)が出た場合は、
  個別対応ではなく、まず「そのファイルが`registerPersistentFile`で
  登録されているか」を確認すること。

## 40. 公開後の対応: プレリリース表記の追加 + 累計アクセスカウンターの恒久化(2026-09-08・23回目)

依頼者から、無事に公開できたとの報告と合わせて2件の依頼があった。
(1) 広告等がまだない状態のため、プレリリースであることをタイトルに
明示してほしい。(2) 累計アクセスカウンターを、ファイル(index.html)を
書き換えても数字が消えず、一生増え続ける仕組みに変えたい。特に
プレリリース版から通常版にファイルを差し替える際に数字が1に
戻らないようにしたい、というもの。

### 40-1. プレリリース表記の追加
- 全7ページ(`index.html`, `highlight-studio-03-app.html`,
  `highlight-studio-07-terms.html`, `highlight-studio-08-privacy.html`,
  `highlight-studio-10-contact.html`, `highlight-studio-11-about.html`,
  `404.html`)の`<title>`タグに「【プレリリース】」を追加。
- 同じく全7ページのヘッダーロゴ(`brand-name`)の右側に、目立ちすぎない
  アンバー色の小さな「プレリリース」バッジ(`.prerelease-badge`)を追加。
  i18n対応済みの4ページ(index/404/contact/about)は
  `data-i18n="common.prereleaseBadge"`経由(`i18n-common.js`に
  `common.prereleaseBadge`キーを追加)、日本語のみの3ページ(app/terms/privacy)
  は固定テキストで直接埋め込み。
- `index.html`のOGP/Twitterカードのタイトル(`og:title`/`twitter:title`)にも
  同様に「【プレリリース】」を追加(SNSシェア時の表示にも反映されるように)。
- **通常版へ切り替える際の作業**: 各ページの`<title>`から「【プレリリース】」を
  削除し、`.prerelease-badge`の`<span>`要素(ヘッダー内、`brand-name`の直後)を
  丸ごと削除するだけでよい。CSSルール(`.prerelease-badge{...}`)は残しても
  無害なので削除は任意。

### 40-2. 累計アクセスカウンターの恒久化
- 従来は「ブラウザのlocalStorageに保存し、開くたびに+1する」方式だった
  ため、(a)ブラウザが変わると0に戻る、(b)`index.html`を書き換える・
  差し替えるたびに数字がリセットされる、という2つの問題があった
  (依頼者が今回問題視していたのはこの(b))。
- 数字をファイル自体やブラウザ内ではなく、外部の無料カウンターAPI
  `countapi.mileshilliard.com`(登録・APIキー不要。以前使っていて
  サービス終了した`countapi.xyz`の後継として稼働している)側に持たせる
  方式に変更した。`index.html`内の固定キー`highlight_studio_site_total_visits`
  に対して`/api/v1/hit/`を呼ぶことで、そのキーに紐づく数字を+1して
  取得している。数字はAPI側に保存されるため、`index.html`を今後何度
  書き換えても、**このキー名を変えない限り**数字は引き継がれ増え続ける。
- **重要(プレリリース→通常版の差し替え時)**: 通常版のファイルでも
  `COUNTER_KEY`(`highlight_studio_site_total_visits`)を同じ値のまま
  使うこと。ここを変えると別カウンター扱いになり数字が1から
  やり直しになってしまう。
- 懸念点として、今回採用した`countapi.mileshilliard.com`も個人が無償で
  運営している小規模サービスであり、以前の`countapi.xyz`と同様に将来
  サービス終了・長期停止する可能性がある(依頼者にも共有済み)。そのため
  取得に失敗した場合は、表示が消えたままになっていた前回の不具合を
  繰り返さないよう、従来の「このブラウザ内だけの参考カウンター
  (localStorage)」に自動でフォールバックするようにした。フォールバック時は
  サイト全体の数字ではなく参考値になる点に注意。
- ラベル文言は、常に「サイト全体」とは限らなくなった(フォールバック時は
  ブラウザ内のみ)ため、`landing.retroCounterLabel`を「このブラウザでの
  累計アクセス」から単に「累計アクセス」に変更した。
- 恒久的な解決を求めるのであれば、依頼者自身のバックエンド(6-1で
  用意した`highlight-studio-backend-skeleton.zip`のFlask叩き台など)に
  カウント用の小さなエンドポイントを1つ立てて、それを叩く方式に
  切り替えるのが最も確実(外部サービスの終了リスクがなくなる)。
  現状は「登録不要・サーバー費用ゼロ」というサイト全体の方針を優先し、
  外部無料APIを採用している。

## 41. 「解析する範囲」設定を撤廃し、常に動画全体を解析対象にする(2026-09-09・24回目)

依頼者から、STEP1にあった「解析する範囲(動画の先頭から): 先頭60秒/120秒/180秒」
という設定を撤廃したいとの依頼があった。理由は、ページ上の選択肢が短い秒数
(実質最大3分)しかない一方で、実際にアップロード対象となるのは5時間規模の
長時間配信アーカイブであり、開始3分以降に起きる盛り上がりがそもそも検出対象外に
なってしまっていたため。依頼者に確認したところ、「動画全体を解析対象にする」
方針で進めることになった(処理が重くなる可能性は依頼者も了承済み)。

### 変更内容(`highlight-studio-03-app.html`)
- STEP1の「解析する範囲」セグメントUI(`rangeSegmented`、60/120/180秒の3択)を
  丸ごと削除。`wireSegmented`の初期化対象リストからも`rangeSegmented`を除去。
- ファイルを選択した直後(`setSelectedFile`)に、隠し`<video>`要素で
  metadataを読み込んで動画の長さ(秒)を取得する`probeVideoDuration(f)`を追加し、
  取得できた長さを`fileDurationSec`に保持。ファイルサイズ表示の隣に長さも
  表示するようにした(例:「1234.5 MB ・ 2:15:30」)。
- 検出処理の入り口(`runDetectAndCrop`)で、従来`rangeSegmented`から取っていた
  `clipSec`(=解析する秒数)を、`getFileDurationSec()`(選択時に取得済みならその値、
  未取得なら検出開始時にあらためて`probeVideoDuration`で取得)で置き換え、
  常に動画の長さ全体を解析範囲として渡すようにした。これにより、
  音量(RMS)解析・映像の動き量解析ともに動画の先頭から末尾まで対象になる。
- `formatTime()`を、1時間以上の場合は「時:分:秒」で表示するように拡張
  (これまでは分:秒のみで、5時間規模の動画だと「300:00.0」のような
  見づらい表示になっていた)。
- ドロップゾーン直下の注意文を「動画は先頭から末尾まで全体を解析対象にします」
  という説明に変更し、5時間規模の長時間アーカイブでは解析だけで数十分かかる
  可能性がある旨を明記。
- 検出失敗時のエラーメッセージから「解析範囲」という表現を削除
  (設定できる項目ではなくなったため)。

### 既知のトレードオフ・懸念点(依頼者にも共有済み)
- 音量(RMS)解析は、動画の音声だけをffmpegで16kHzモノラルwavに変換してから
  ブラウザ内でデコードする方式のまま(`extractAudioSnippet16k`は変更なし)。
  ただし対象が動画全体になったため、5時間の動画だと音声データだけでも
  ブラウザのメモリ上に1GB前後になる見込みで、端末のメモリ状況によっては
  失敗する可能性がある。
- 映像の動き量解析(`computeVideoMotion`)は、対応ブラウザでは4倍速の
  疑似再生(`requestVideoFrameCallback`)でフレームを拾う方式のため、
  5時間の動画だと解析だけで理論上75分程度かかる計算になる
  (非対応ブラウザ向けのシーク方式フォールバックはさらに遅い)。
  体感の待ち時間が長くなる点は、進捗表示(残り時間の目安)はあるものの
  UI側の抜本的な改善はできていない。
- 今回は`tools/verify_app_html.py`(Playwrightでのブラウザ動作確認)は
  実行していない。次回このページを触る際は、`rangeSegmented`関連の
  スタブ・アサーションが残っていないか確認したうえで実施すること。

## 42. 長時間アーカイブでの検出失敗(File could not be read)への対応・音声解析のffmpeg依存を撤廃(2026-09-09・25回目)

41章で「動画全体を解析対象にする」に変更した直後、依頼者から実際に長時間の
動画で試したスクリーンショットが届いた。「映像解析:残り約105分16秒(50%)」の
表示と同時に「検出処理に失敗しました / File could not be read! Code=0」という
エラーが出ていた。調査の結果、原因は2つあった。

### 42-1. 見た目の不具合(進捗表示が壊れて見える)
エラー発生時、`startDetect()`のcatchブロックが`waveTime.textContent = ''`を
していたにもかかわらず、直前のステージの`startProgress()`が仕込んだ
`setInterval`(0.5秒ごとに`waveTime`を上書きする)を止めていなかったため、
エラー表示の裏で古い進捗が上書きされ続けていた。catchブロックの先頭で
`stopProgress(waveProgressFill)`を呼ぶように修正。

### 42-2. 本質的な原因(なぜ検出処理自体が失敗したか)
`File could not be read! Code=0`は、ffmpeg.wasm(`@ffmpeg/ffmpeg@0.11.6`)の
`fetchFile()`内部で使われている`FileReader`が、渡されたFile(動画ファイル)を
読み込めなかったときに出るエラー。これは`ensureFfmpegLoaded()`が
`ffmpeg.FS('writeFile', 'input.mp4', await fetchFile(file))`という形で、
**動画ファイル全体を一度にメモリへ読み込んでffmpegの仮想ファイルシステムに
書き込む**処理をしていることに起因する。5時間規模のアーカイブは数GB〜
十数GBになることがあり、その全体を一括でメモリに載せようとしてブラウザの
メモリ上限に達し失敗していた。

これは41章での「動画全体を解析対象にする」変更そのものの不具合というより、
「検出(音量解析)のために動画全体をffmpegに読み込ませる」という、
以前からあった設計が長時間ファイルでは無理をしていた、という話。
依頼者に3方向の対応案(音声解析のffmpeg依存を先に撤廃/ファイルサイズ上限を
設けて事前警告/様子見)を提示し、「音声解析だけ先にffmpeg依存をなくす」を
選んでもらった。

### 変更内容(`highlight-studio-03-app.html`)
- 新関数`computeAudioAndMotionByPlayback()`を追加。映像の動き量解析
  (`computeVideoMotionByPlayback`と同じ、4倍速再生+`requestVideoFrameCallback`)
  と同じ1回の再生の中で、Web Audio APIの`AudioContext.createMediaElementSource()`
  + `AnalyserNode.getFloatTimeDomainData()`を使って音量(RMS)も同時に取得する
  ように変更。これにより、**検出段階(音量解析・映像解析)ではffmpegを
  一切使わなくなった**(動画ファイル全体をffmpegに読み込ませる必要がなくなった)。
  `source`は`destination`に繋いでいないため、この経路からは音は出ない
  (`videoEl.muted = true`と合わせて二重に無音)。
- `computeAudioAndMotion(file, dur, stepSec, onProgress)`を新設し、
  `requestVideoFrameCallback`とWeb Audio APIの両方が使える環境
  (Chrome/Edge/Safariの最新版など、対応ブラウザとして案内している環境)では
  上記の同時解析方式を使い、非対応の古いブラウザ向けのみ、従来通り
  「映像はシーク方式(`computeVideoMotionBySeek`)・音量はffmpegでの
  全体抽出(`computeAudioRms`)」にフォールバックするようにした。
  **フォールバック経路に限っては、5時間級の大きなファイルで今回と同じ
  メモリ不足が再発する可能性が残っている**(対応ブラウザを使っている
  限りは発生しない)。
- 使われなくなった旧`computeVideoMotion()`(映像単体の新旧振り分け
  ラッパー)は削除。`computeVideoMotionByPlayback`/`computeVideoMotionBySeek`
  自体はフォールバック経路や音声解析と共有する形でそのまま残っている。
- `runDetectAndCrop()`内で、`ensureFfmpegLoaded(preset)`(=動画ファイル
  全体をffmpegへ書き込む重い処理)の呼び出し位置を、検出フェーズの前
  から**クリップ切り出し(stage-crop)の直前**に移動。これにより、
  検出だけで完結する場合(何らかの理由で検出が失敗した場合や、検出結果を
  見てそこで作業をやめた場合)は、動画全体をffmpegに読み込む重い処理が
  一切発生しなくなった。
- ステージ表示(画面上部の「音量解析→映像解析→切り出し」の3つの丸
  アイコン)は、検出が1回の再生で同時に行われるようになったことに合わせ、
  「音量解析」と「映像解析」の両方を同時に「進行中(active)」表示にする
  ように調整(`setStage('stage-audio')`実行後に`stage-motion`要素にも
  手動で`active`クラスを付与)。切り出し段階に進む際の`setStage('stage-crop')`
  で両方とも自動的に「完了(done)」表示になる。
- 検出失敗時のエラーメッセージに、`File could not be read`パターンを
  検知した場合だけ「動画のサイズに対してブラウザのメモリが足りなかった
  可能性が高い」という趣旨のヒントを追加で表示するようにした
  (42-1の表示修正と合わせて実施)。

### 残っている制約・次にやるなら
- **クリップの切り出し(stage-crop)自体は、依然として動画ファイル全体を
  ffmpegに読み込む必要がある**(`-ss`/`-i input.mp4`でシークするため)。
  今回の対応で「検出」までは大きなファイルでも成功しやすくなったが、
  検出が終わって実際にクリップを切り出す段階で、同じ
  `File could not be read`が起きる可能性はまだ残っている。依頼者には
  この点を伝え、次の対応候補として挙げてある。
- 次にこの制約に対応するなら、(a)ファイルサイズに応じた事前警告の追加、
  (b)切り出しも動画全体をffmpegに読み込まず、`file.slice()`などで
  対象区間に近いバイト範囲だけを渡す方式への変更、のどちらかを検討する
  のがよい((b)はMP4のコンテナ構造上、時間指定で正確にバイト境界を
  求めるのが簡単ではなく、相応の調査が必要)。
- `tools/verify_app_html.py`は今回も未実行。次回触る際に、
  `computeAudioAndMotion`/`computeAudioAndMotionByPlayback`まわりの
  動作確認も合わせて行うこと。

## 43. 大きい動画ファイルまわりで「分かっている・起こりそうな」不具合の先回り対応(2026-09-09・26回目)

依頼者から「分かってるエラーや起こりそうなエラーは先に潰して」と依頼があった。
42章で検出段階のffmpeg依存は撤廃したが、クリップの切り出し(stage-crop)は
依然として動画ファイル全体をffmpegに読み込む必要があり、同じ種類の失敗が
再発しうる状態だった。この経路まわりで、実際に問題が起きる前に対応できる
ものを先に直した。

### 43-1. fetchFile()の既知の不具合を回避(`ensureFfmpegLoaded`)
ffmpeg.wasm(`@ffmpeg/ffmpeg@0.11.6`)付属の`fetchFile()`は内部で
レガシーな`FileReader`を使っており、ファイルサイズが2GB前後を超えると
`File could not be read! Code=0`で失敗する既知の不具合が
ffmpeg.wasm本体のissueで複数報告されている(依頼者が42章で実際に
遭遇したエラーそのものと一致)。`fetchFile(file)`をブラウザ標準の
`File.arrayBuffer()`(`new Uint8Array(await file.arrayBuffer())`)に
置き換えた。これは「FileReader特有の不具合」を回避するものであり、
ブラウザのArrayBuffer1個あたりのサイズ上限(Chromeで概ね2GB程度)
そのものを引き上げるものではない点に注意(43-3参照)。

### 43-2. 検出処理が無反応のまま固まる可能性への対策
新設した`computeAudioAndMotionByPlayback`(42章で追加)には、以下の
潜在バグがあった。
- `videoEl.play()`が失敗した場合、`reject`は呼ばれるが`AudioContext`が
  閉じられないまま残ってしまう。「もう一度試す」を繰り返すと、
  ブラウザのAudioContext数の上限に達して別のエラーを起こしうる。
- 映像フレームごとのコールバック(`onFrame`)内で予期しない例外
  (例:Canvasの`getImageData`が何らかの理由で失敗するなど)が起きた場合、
  例外が握りつぶされてPromiseが一生解決も却下もされず、検出処理の
  進捗が固まったまま二度と動かなくなる。
どちらも、該当箇所で確実に`AudioContext`を閉じたうえで`reject`するように修正し、
呼び出し元(`computeAudioAndMotion`)の「失敗したら従来方式にフォールバックする」
処理に確実に落ちるようにした。

### 43-3. ファイルサイズが大きい場合の事前警告を追加
クリップ切り出し(`ensureFfmpegLoaded`)は、今後も「動画ファイル全体を
一度にメモリへ読み込む」という制約そのものは残る(根本的に解消するには
`@ffmpeg/ffmpeg@0.12.x`系への移行が必要で、APIが全く異なる
(`createFFmpeg()`/`ffmpeg.FS()`/`ffmpeg.run()` → `new FFmpeg()`/
`.writeFile()`/`.readFile()`/`.exec()`/`.mount()`、Web Worker越しの
呼び出しに変わる)ため、今回は見送り、通知として`checkFileSizeRisk()`を
追加するに留めた)。
- `checkFileSizeRisk(f)`: ファイルサイズが1.5GBを超える場合に、
  「検出はできるが、切り出しの段階でメモリ不足により失敗する可能性がある」
  という趣旨の注意文を返す(あくまで目安で、厳密な線引きではない)。
- ファイル選択直後(`setSelectedFile`)に、ドロップゾーン下へアンバー色の
  注意文(`#dropzoneSizeWarning`、新設)として表示し、ログにも残す。
- クリップ切り出し開始直前(`ensureFfmpegLoaded`を呼ぶ直前)にも同じ
  チェックをログへ出し、実際に失敗した場合にエラーメッセージだけでなく
  事前の注意ログからも原因を追いやすくした。

### 検討したが見送ったもの: WORKERFS
ffmpeg.wasmには、動画ファイルをメモリへコピーせずに読ませられる
「WORKERFS」というマウント方式があり(GitHubのdiscussionでは13.2GBの
ファイルの読み込みに成功した報告もある)、本質的な解決策として有力だが、
**この機能は`@ffmpeg/ffmpeg@0.12.x`系(`new FFmpeg()` + `.mount()`)の
APIであり、現在このプロジェクトが使っている0.11.6系(`createFFmpeg()` +
`ffmpeg.FS(method, ...)`)には無い**(0.11.6の`ffmpeg.FS()`は
writeFile/readFile/unlinkなど限られたメソッドのプロキシで、
`mount`やファイルシステム種別の参照を渡す手段が無い)。0.12.x系への
移行はAPI呼び出し規約(ロード方法・全ffmpeg呼び出し箇所・エラーハンドリング)
を丸ごと書き換える規模の作業になるため、今回は見送った。今後、
1.5GBを大きく超える動画の切り出しが実運用で頻発するようなら、
このタイミングで0.12.x系への移行を検討するのがよい。

## 44. 全体デバッグ・改善パス(2026-09-09・27回目)

依頼者から「デバッグして。よくできるところはアップデート」という
包括的な依頼があった。特定のエラー報告ではなく、コード全体を見直して
気付いたものを直した。

### 44-1. 【バグ】5時間規模の動画で波形バーのDOM要素が数万個になる
`renderWaveBars()`は、`combined`配列(0.5秒刻みのため5時間の動画だと
最大36,000要素)の1要素につき1個の`<span>`をそのまま生成していた。
表示領域(`.wave-bars`、`overflow:hidden`)に対して明らかに過剰な数の
DOM要素で、長時間動画では描画がもたつく・固まる原因になりうる。
最大300本にまとめてから描画するように変更(`WAVE_BAR_MAX_COUNT`)。
まとめる際は各バケット内の最大値を採用し、ピーク(選択区間)の
見た目上の視認性は維持している。

### 44-2. 【不整合】プレリリース中なのに「広告を表示しています」と出る
書き出し開始時・ダウンロード時に挟んでいる`showAdGate()`は、
広告ネットワーク未確定のためのモック実装で、実際には何も広告を
表示しないまま「広告を表示しています。5秒後に続行できます。」という
文言で5秒待たせていた。40章で「プレリリース(広告などまだ無し)」と
サイトに明記した経緯があるため、実態のない広告をあるかのように
見せてユーザーを待たせるのは不誠実かつ無意味と判断し、実際の広告
ネットワークを導入するまではゲート自体を素通りする(`AD_GATE_ENABLED = false`)
ように変更した。導入時はこの定数を`true`に戻すだけで、元のモック
カウントダウン表示が復活する。

### 44-3. 【改善】エラーメッセージの文言をstartDetect/startBurnで共通化
42章で追加した「File could not be readの場合はメモリ不足の可能性を
案内する」処理が`startDetect`にしか無く、`startBurn`(書き出し段階。
こちらも`ensureFfmpegLoaded`を経由するため同じ理由で失敗しうる)には
無かった。`describeErrorWithHint(err)`として共通化し、両方から使う
ようにした。あわせて、書き出し段階の文言に「動画ファイルを変えるか」
という(書き出し段階では当てはまらない)表現が残っていたのも修正。

### 確認したが問題なしと判断したもの
- `findTopWindows`の計算量: 0.5秒刻み×5時間(36,000要素)×最大本数ぶんの
  ループになるが、実際の演算量は数十万回程度でボトルネックにはならない。
- STEP4(字幕焼き込み・書き出し)・STEP3(音声認識)は、いずれも
  切り出し済みの小さいクリップ(`cropped_N.mp4`)単位で処理しており、
  動画全体を扱うことによる大きいファイル特有のリスクは無い。
- `runBurnAll`側のffmpeg呼び出しは、42章で見つけたのと同種の
  「エラー時に進捗タイマーが残ったままになる」不具合は無かった
  (すでに各クリップのループ内で`try{...}finally{stopProgress(...)}`に
  なっており、個別に直す必要はなかった)。

### 今回は手を付けなかったもの(気付いたが優先度が低いと判断)
- `computeAudioAndMotion`のフォールバック経路(非対応の古いブラウザ向け、
  滅多に通らない)では、映像の動き量解析と音量解析が別々に行われるため、
  進捗バーが0→100%を2回繰り返す見た目になる。実害はほぼ無いため、
  他の対応を優先し今回は未対応。

## 45. 追加デバッグパス(2026-09-09・28回目)

「更にアップデート」という依頼を受け、続けて見直しを行った。

### 45-1. 【バグ】STEP2の説明文が、撤廃済みの「解析する範囲」機能に言及したまま
41章で「解析する範囲(先頭60/120/180秒)」の選択UIを撤廃し、常に動画全体を
解析するように変更したが、STEP2パネルの説明文
「音量と映像の動き量から、**指定した範囲**の中で盛り上がっている区間を…」が
そのまま残っており、すでに存在しない設定を指す文言になっていた。
「動画全体の中で」に修正。全ページを検索し、同種の古い文言
(「先頭60秒」等)が他に残っていないことも確認済み。

### 45-2. 【不整合】og:titleにプレリリース表記が反映されていないページがあった
40章でタイトルタグには全7ページ「【プレリリース】」を付けたが、
og:title(SNSでリンクを共有した際に表示されるタイトル)には
index.htmlしか反映していなかった。残り5ページ
(03-app/07-terms/08-privacy/10-contact/11-about)のog:titleにも
同じ表記を追加し、`<title>`と一致させた。404.htmlは`noindex`かつ
そもそもog:title自体を持たないページのため対象外(意図通り)。

## 46. 【重大】本番ユーザーに「AD PLACEHOLDER」がそのまま見えていた不具合を修正(2026-09-09・29回目)

3度目の「更にアップデート」依頼で見直し。今回見つけたのは、これまでで
一番実害が大きいと思われるもの。

`index.html`(トップページ、比較セクションの下)と
`highlight-studio-03-app.html`(STEP4・書き出し画面)に、それぞれ

```
AD PLACEHOLDER — 300x250 / responsive
広告ネットワーク導入後、ここに広告タグを差し込んでください
```
```
AD PLACEHOLDER — 728x90 / responsive
広告ネットワーク導入後、ここに広告タグを差し込んでください
```

という、開発時に仮置きしたダミー枠が、非表示化も削除もされないまま
**そのまま本番ユーザーに常時表示されていた**。プレリリース(広告なし)と
案内している内容と真っ向から矛盾するうえ、そもそも開発用のメモ書きを
訪問者に見せてしまっている状態だった。44章で見つけた「広告ゲート」の
モック表示は`AD_GATE_ENABLED`フラグで対応済みだったが、この2箇所の
静的なダミー枠は同フラグの対象に入っておらず、見落とされていた。

### 対応
- `index.html`側は、HTML側に直接`display:none`を指定して非表示化
  (このページには機能フラグの仕組みが無いため)。
- `highlight-studio-03-app.html`側は、既存の`AD_GATE_ENABLED`定数と
  同じフラグで表示・非表示を切り替えるようにJS側で実装
  (`adSlotExport.style.display = AD_GATE_ENABLED ? '' : 'none'`)。
  今後広告ネットワークを導入する際は、`AD_GATE_ENABLED`を`true`に
  変更すれば、書き出しページの広告枠も同時に元通り表示される。
- サイト内の他のページ(404/terms/privacy/contact/about)には同種の
  プレースホルダーは無いことを確認済み。

## 47. 追加デバッグパス・小さな不整合の解消(2026-09-09・30回目)

4度目の「更にアップデート」依頼。今回は小粒だが確実な不整合を2件解消。

- `highlight-studio-10-contact.html`: 「お問い合わせ」ページの案内文が、
  日本語版では「不具合のご報告やご要望など」なのに、英語版だけ
  「bugs, feature requests, or questions about pricing plans」と
  存在しない「料金プラン」への言及が入っていた。利用規約
  (`highlight-studio-07-terms.html`)で「ユーザーに料金の請求を行うことは
  ありません(本サービスは無料)」と明記している通り、そもそも料金プラン
  自体が存在しないため、英語版の文言を日本語版に合わせて修正した。
- `index.html`: `.plan-pill`というCSSクラスが定義されていたが、
  対応する`class="plan-pill"`を持つHTML要素がどこにも存在せず、
  完全に未使用の死んだCSSだった(表示上の実害は無いが、コードの
  紛らわしさの元になる)。定義本体と、それを非表示にする専用の
  メディアクエリ行の両方を削除した。

### 確認したが変更しなかったもの
- `index.html`のヘッダーで、`.prerelease-badge`が非常に狭い画面
  (320〜375px程度)で窮屈にならないか気になったが、実際のブラウザで
  検証する手段が無く、根拠のない見た目の調整を加えるとかえって
  改悪するリスクがあるため、確証を得られる情報(実機・実ブラウザでの
  見え方)が無い限りは触らないことにした。実際に見て気になるようであれば
  教えてほしい。

## 48. 精度・処理時間の実用面での高性能化(2026-09-09・31回目)

依頼者から「精度や、処理能力、処理時間など実用的な部分の高性能化」という
依頼があった。バグ探しではなく、既に動いている検出・エンコード処理そのものの
質を上げる対応。

### 48-1. 【精度】音量(RMS)を「瞬間サンプリング」から「連続積算」に変更
`computeAudioAndMotionByPlayback`の音量解析は、以前は0.5秒のステップ境界に
到達するたびに`AnalyserNode`で「その瞬間の約46ms分(2048サンプル)」だけを
読み取っていた。境界と境界の間(残り約454ms)に鳴った音量変化は
まったく見ておらず、たまたま境界の瞬間が静かだった/うるさかっただけで
結果が左右されうる状態だった。
`ScriptProcessorNode`に切り替え、再生中に流れてくる音声データを継続的に
受け取って二乗和とサンプル数を蓄積し、ステップ境界に来たタイミングで
「前回の境界からここまでの平均」としてRMSを求めるようにした。
これにより、ステップ間で起きた音量変化も取りこぼさなくなった。
(ScriptProcessorNodeは非推奨API だが、対応ブラウザとして案内している
最新のChrome/Edgeでは引き続き問題なく動作する。destinationに繋がないと
発火しないブラウザ対策として、音量0のGainNode経由でdestinationに接続している。)

### 48-2. 【精度】映像の動き量も同様に「瞬間サンプリング」から「連続積算」に変更
動き量(canvasのフレーム差分)も、以前はステップ境界の瞬間の1フレームと
直前に記録した1フレームだけを比較していた。`requestVideoFrameCallback`は
実際には(4倍速再生中であっても)ステップ境界よりずっと高い頻度で
呼ばれているため、境界間で起きた素早いカット割りなどの動きを取りこぼして
いた。表示されるフレームごとに前フレームとの差分を計測して積算し、
ステップ境界でその区間の平均値を採用するように変更。音量・映像とも
「その区間の代表値」を見るようになったことで、単発のノイズに引っ張られ
にくくなり、検出精度が上がる見込み。

### 48-3. 【処理時間】本番相当プリセットのエンコード速度を改善
クリップの切り出し・字幕焼き込みで使っているx264の`preset`を、
本番相当(`production`)プリセットで`medium`→`fast`に変更した
(`crf`(画質目標)は20のまま変更していない)。x264では同じcrfのまま
presetだけ1段階速くしても体感できるほどの画質差はほぼ無く、ファイル
サイズがわずかに増える程度で、エンコード自体の所要時間は数十%短縮できる
(一般的に知られたトレードオフ)。テスト用プリセットは元々`ultrafast`
(最速)のため変更なし。

### 検討したが見送ったもの
- **検出時の再生速度(`playbackRate`)を4倍からさらに上げる**: Chromeは
  仕様上16倍速まで対応しているため、単純に処理時間を縮められる可能性は
  ある。ただし、Firefox(Gecko)は4倍速を超えると音声を強制的にミュート
  する仕様があるとされ、そのミュートが「スピーカー出力だけ」ではなく
  「Web Audio APIに渡るデータ自体の無音化」を意味する場合、音量解析の
  精度がFirefoxで丸ごと壊れるリスクがある。実際の挙動を実機で検証する
  手段が無く、精度を犠牲にしてまで速度を上げるのは依頼の趣旨(精度も
  重視)に反するため、今回は見送った。もし処理時間をさらに詰めたい場合、
  対応ブラウザをChrome/Edgeに限定した上で慎重に検証しながら上げる、
  という進め方が安全。
- **combined(音量0.6:映像0.4)の重み付けの調整**: 実データでの検証手段が
  無いまま数値を変えても「本当に精度が上がったか」を確認できないため、
  今回は触らなかった。

## 49. 処理時間の追加高速化: 字幕なし書き出しの無駄な再エンコードを解消(2026-09-09・32回目)

引き続き「処理時間」まわりを見直し、書き出し段階(STEP4・`runBurnAll`)に
明確な無駄を見つけた。

### 49-1. 字幕を焼き込まない場合でも、毎回libx264で再エンコードしていた
書き出し段階は、字幕(drawtext)を焼き込むかどうかに関わらず、常に
`-c:v libx264 -preset ... -crf ...`で映像を再エンコードしていた。
しかし、字幕もハイライトボックスも無い(=`drawtextParts`が空で
`vfArg`が無い)場合、切り出し段階で作った映像に対して**何も変更を
加えていない**。それにもかかわらず、まったく同じ映像をもう一度
最初からエンコードし直すという、時間のかかる処理を無駄に行っていた。
`vfArg`が無い場合は`-c:v copy`(再エンコードせずそのままコピー)に
切り替え、単純なコンテナの詰め替えだけで済むようにした。
字幕・ハイライトボックスのどちらかが有効な場合は、従来通り
libx264で再エンコードする(そうしないと焼き込みが反映されないため)。
音声はもともと`-c:a copy`のままで、この点は変更していない。

### 49-2. 最終出力ファイルに`-movflags +faststart`を追加
書き出しの最終ファイル(`final_N.mp4`。実際にダウンロード・共有される
ファイル)に、web配信動画の定番設定である`-movflags +faststart`を
追加した。MP4のメタデータ(moovアトム)をファイル先頭に置くことで、
ブラウザでのプレビュー再生の始まりが早くなるほか、SNSなど他サービスへ
再アップロードした後の再生互換性・体感速度も上がる。エンコード自体には
ほぼ影響しない(単純なコンテナ内配置の違いのため、追加の処理時間は
無視できるほど小さい)。

これにより、字幕なしで書き出す使い方(検出結果をそのまま縦動画として
欲しいだけのケース)では、書き出し段階の所要時間が大きく短縮される
見込み。
